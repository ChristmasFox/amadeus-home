# Canonical context — 2026-09-26

Read this with `docs/CURRENT_TASK.md`; inspect Git and live state before runtime operations. Read a Goal document when a new active Goal is named, not every historical report on startup.

- Current released product version: `VERSION=1.6.0` on canonical `main`. The completed Post-Voice performance Goal is `docs/AMADEUS_POST_VOICE_ENGINEERING_PERFORMANCE_GOAL.md`, amended by explicit owner §12 cancellation of further B reference testing. Four earlier B benchmark cells remain safety-incomplete in the numeric report, not passing.
- OpenClaw 2026.9.4 is the **sole Agent runtime** with native PUBG/Amadeus plugins, deterministic domain/presentation and one owner outbox. No LangBot/n8n/old Runtime, second planner/sender or keyword router.
- On M204 OrbStack `nyannyan` CasaOS, the immutable OpenClaw 1.6.0 image is source-tagged at `c730b49`. The single Mac TTS LaunchAgent uses original protected A (~46s) reference, pinned community MLX 1.7B Base 8-bit, Auto language, Interactive scheduling and the unchanged bounded OpenAI-compatible contract. The owner accepted real post-release WhatsApp voice/typed quality, Japanese naturalness and normal volume/rhythm.
- Source of truth: Git `main` for released source; host-specific configuration resolves from local `infra/host-profile.env` via `scripts/host-profile.sh`, and secrets/reference/media/weights stay outside Git. Protected rollback paths and evidence: `docs/PROJECT_STATE.md`, `docs/reports/AMADEUS_TTS_PERFORMANCE_2026_09.md`, and dated `.agent/checkpoints/`.
- Development: `pnpm workflow:plan`, focused tests/typecheck and `git diff --check` for FAST/RUNTIME; no automatic Docker or deploy. Full `pnpm test` belongs to CI/release/explicit requests. Run `pnpm check:secrets` before commit. `scripts/deploy-openclaw.sh` defaults to dry-run; production apply requires explicit flags, immutable image, protected checkpoint and real acceptance.
- Operational watch: MLX cold/real `vmmap` peak 18.4 GiB on 24 GiB Mac, with transient swap growth; keep exact external A/MPS rollback and monitor actual memory pressure. Optional media adapter absence and daemon log-policy warning are known, not a Voice fallback. B audit history is preserved under Git tag `audit/tts-b-reference-20260926`; it is not an active task.
