# Current Task — Post-Voice Engineering & TTS Performance

Date: 2026-09-26. Authoritative scope: `docs/AMADEUS_POST_VOICE_ENGINEERING_PERFORMANCE_GOAL.md`.

- Phase 0: production 1.5.9 source and rollback verified; Voice branch fast-forwarded into canonical `main` and pushed. New short-lived branch: `work/post-voice-engineering-performance`.
- Phase 1: completed context/ceremony reduction; historical diaries retained. Architecture/workflow/secrets/diff checks passed in commit `477771a`.
- Phase 2: offline `verify:voice`, `verify:amadeus`, `verify:openclaw-patch` and explicit `accept:voice` entry added; baseline ad-hoc verification 4.01s, new entry 4.05s on this Mac (6 Python tests, 1 optional encoder skip; 4 selected plugin tests plus patch fixtures/typecheck). No model download, MPS, Docker, deploy. Targeted workflow ownership and offline tests pass.
- Phase 3: source instrumentation now separates lock wait, upstream model boundary, WAV serialization, encode, total and RTF. Offline fake-model test covers lock contention; real MPS instrumentation candidate applied with protected checkpoint; one warm synthetic MP3 sample shows queue 0ms, model 11.07s, WAV 2.5ms, encode 0.366s, RTF 3.643. Bounded single inference worker + one waiting slot implemented in source after historical lock/timeout evidence; full queue and >5s pending wait fail closed as `503 tts_busy`, shutdown tests added. Candidate applied on M204 with protected checkpoint and source SHA match. Synthetic three-request contention returned 200/503/503 with busy in ~5.2s/2ms, health 200; owner WhatsApp acceptance still pending. Profile/language/model unchanged. LaunchAgent bootstrap needed one manual recovery after error 5. Phase 4 private A–E profiles prepared. A Auto/Japanese × three buckets completed with five warmed runs per config; preliminary A short p50 ~4.5s direct engine, not end-to-end or quality. B ICL Auto short stalled beyond 120s on its third request; only the experimental process was terminated, production health stayed 200. A 110s per-sample supervisor is now continuing the remaining configs and preserving failures. Phase 4 matrix: A/C/D/E completed 5 warmed runs per input/language config; B had four supervised 110s timeouts and is unsafe/incomplete, one B Auto short retry completed with high variance. Owner audition pending. Controlled LaunchAgent ProcessType A/B (Background vs Interactive, same A/Auto/short and service SHA) measured p50 11.21s vs 4.55s over five HTTP requests; 20 Interactive runs p50 4.48s/p95 5.28s, reversal Background five p50 10.39s, restored Interactive five p50 4.50s. Model/profile/language/service SHA unchanged; this strongly supports launchd scheduling as a major latency cause. Still no owner/WhatsApp acceptance or formal release. Phase 5 isolated community MLX 1.7B 8-bit PoC completed 12 A/D × Auto/Japanese × three-bucket configs (five warm successes each): A short p50 3.33s vs MPS 4.53s, A long 7.94s vs 16.26s; D Japanese short 1.23s vs 2.72s. MLX A peak ~10–12 GiB, D ~6–7 GiB vs MPS D ~5–6 GiB; first cold load ~23.7s. Experimental engine boundary exists but MPS remains default. Owner confirms D loses Kurisu character and MLX sacrifices timbre; selects original A/MPS/Auto despite D saving ~2s. D/MLX rejected for this release, C/E not owner-approved. Live service already has A/MPS/Auto, so no profile/backend switch. Phase 6 source: Voice compatibility script shrank 419→264 lines; pure Japanese policy, lease/queue helper and markers moved to independently tested modules. Pinned OpenClaw 2026.9.4 core/TTS patched output SHA unchanged; module tar/CLI and anchor regression pass. Dockerfile/runtime deploy now carry the modules; no container build/apply yet. Phase 7 host BuildKit: no-op Voice patch change rebuilt ffmpeg apt before reorder (39s wall, apt 22.6s); after reorder patch-only build 3s with apt/glibc/npm cached, plugin dist-only 1s with those layers cached. Network-none image ABI smoke and affected-only build-auto passed; no CasaOS switch. Phase 8 optional Opus shortcut not selected: measured MP3 encode is ~0.03–0.37s versus multi-second model variance; without proven native WhatsApp PTT MIME/duration/channel equivalence, preserve the existing MP3→WhatsApp Opus path. OpenClaw candidate `c756194` applied with affected-only build and protected CasaOS checkpoint `amadeus-openclaw-20260926104238`; healthy/zero restarts, WhatsApp linked, both Voice markers present, image ABI smoke passes. TTS remains A/MPS/Auto/Interactive. Real post-candidate voice+typed/one-PTT owner acceptance, TTS host source now matches Git `service.py` (same A/MPS/Auto/Interactive, health 200), protected pre-sync checkpoint and five fixed MP3 smokes p50 4.64s/p95 5.17s. Two real post-candidate WhatsApp audio inbounds had one corresponding media-send event each (22.58s and 20.74s inbound→media; TTS 9.33s and 9.91s). Owner explicitly confirms no issue with one Japanese PTT, nonduplicated visible text and typed-not-triggering-TTS after the candidate. Final machine-checkable report, release verification, immutable release build/CasaOS switch and final checkpoint remain.
- Do not change TTS model/profile/language/backend before the baseline and measurement gates. No timeout increase, new Voice features, or second runtime.

Open items requiring independent evidence: final performance report, release gates, immutable production switch and post-release health/WhatsApp acceptance; older 1.5.9 group guard handset claim remains historical unless separately confirmed. The optional media adapter is absent; strict doctor reports this known non-Voice failure. Historical details remain in `docs/history/` and `.agent/checkpoints/`.
