# 2026-09-25 — Native bilingual voice reply contract (source only)

- New owner preference: for an inbound voice turn with a non-Chinese spoken response, append one concise faithful Chinese text sentence; if the user explicitly requests Chinese speech, no redundant Chinese text. Typed input remains text-only. This extends the current 1.5.3 voice capability; no new Agent, channel sender, router or external translation service.
- Pinned OpenClaw 2026.9.4 `maybeApplyTtsToPayloadCore` separates `visibleText` from explicit `ttsText` when model text uses `[[tts:text]]...[[/tts:text]]` and `tts.modelOverrides.allowText` is enabled. The WhatsApp plugin audio branch sends PTT and, when given a visible caption, an additional text reply via the same transport. This is source inspection, not proof of actual delivery.
- Added Amadeus `voice-reply` Skill in plugin manifest and overview; it instructs a single final payload with one visible Chinese sentence and one audio-only Japanese block after the normal tool loop. Config retains `tts.auto=inbound`, `mode=final` and global Agent deny for `tts`/`message`. Pinned parser fixture verifies Chinese visible/Japanese spoken separation and ordinary Chinese without directive; config/schema/architecture checks passed.
- **Pending:** commit, immutable OpenClaw image build due plugin Skill source, protected candidate apply, preflight Skill visibility, real WhatsApp voice acceptance: exactly one PTT plus one Chinese text after non-Chinese answer, no extra message for explicit Chinese, meaningful summary, TTS-failure text fallback and no duplicate side-send. Also existing A–L and 1.5.3 release gates.
