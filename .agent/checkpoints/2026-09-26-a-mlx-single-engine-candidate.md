# A+MLX single-engine candidate — 2026-09-26

- Owner accepted the exact A (~46s original reference) + MLX 1.7B 8-bit direct listening sample and requested implementation. Source commits `6749e9e`/`156d9cb` declare `productionEngine=mlx`, pinned community source/model/dependencies and protected asset SHA manifest; default service API, Auto language, one bounded worker, 120s external timeout, Interactive launchd class and original profile remain unchanged. Focused offline tests, architecture, secrets and diff checks passed. No second Agent/sender/TTS process or fallback was added.
- Protected pre-switch checkpoint: `/Volumes/Avalon/backups/operation-skuld/qwen3-tts/protected-performance/pre-a-mlx-candidate-20260926T123834Z`, 0700 with eight 0600 service/plist/requirements/profile/token files and verified SHA manifest. An earlier incomplete attempt was quarantined with `INCOMPLETE-` prefix and is **not** a rollback point. Asset root `~/Library/Application Support/Amadeus/speech/mlx-poc` has verified pinned source/model/package checksums; reference/token values were never logged or committed.
- Explicit `manage-qwen3-tts.sh --apply-plist-only --engine mlx` succeeded. The old MPS PID 40515 exited before new PID 53507 bootstrapped; same `com.amadeus.qwen3-tts` LaunchAgent and port 18792, no concurrent resident engine. Live plist says `AMADEUS_TTS_ENGINE=mlx`; Git service/engine module SHA already matched live. Health ready; OpenClaw/Product Radar healthy, restart 0. No OpenClaw/9Router route or channel config changed.
- Protected local HTTP fixed Japanese MP3 acceptance: five short requests all valid (p50 3.357s, p95 4.636s including first-use); twenty further short requests all valid, p50 **3.296s**, p95 **3.516s**, min 3.185s, max 3.528s. Five normal (~50-char) requests all valid, p50 **5.424s**, p95 **5.568s**. Model generation dominates; queue approximately zero, MP3 encode tens of ms. These are not real WhatsApp p50/p95 or owner handset acceptance.
- Memory gate is mixed: previous live A/MPS `vmmap` footprint ~9.3 GiB and swap used ~3.63 GiB. New MLX cold startup `vmmap` peak **17.5 GiB** and macOS swap briefly rose to ~6.75 GiB; afterward idle footprint ~3.3 GiB and short-run footprint varied (up to ~12.2 GiB immediately after a batch), `memory_pressure -Q` later ~70–80% free and swap used ~6.76 GiB with no active swap churn in a single sampled `top` interval. This is **not** proof of zero memory risk; keep observing during real traffic, and rollback if pressure, swap growth, instability or handset timbre regress.
- Real WhatsApp voice/typed/PTT quality and post-release acceptance are pending. Do not release or claim complete based only on synthetic HTTP results. Rollback: restore the named checkpoint files with 0600 and explicitly `manage-qwen3-tts.sh --apply-plist-only --engine mps` (or restore the saved MPS plist), wait for health and source SHA, then perform a real owner voice smoke. Never alter the private A reference/token or enable simultaneous MPS+MLX serving.
