# Phase 5 MLX 1.7B community backend PoC — 2026-09-26

- Isolated external PoC root: `~/Library/Application Support/Amadeus/speech/mlx-poc` (private cache/model; no weights or samples in Git). `Blaizzy/mlx-audio` community source commit `4ab7e6f7dedd69a136cfaa318c5dc8aed5119446`, package 0.5.6, MLX 0.32.2, transformers 5.17.0; public `mlx-community/Qwen3-TTS-12Hz-1.7B-Base-8bit` model revision `e7dd0585652209fa0d7783659aad4e8a324de11c` (~3.1GB). Not an upstream Qwen official backend. Separate venv; production service/LaunchAgent remained MPS and health 200.
- Protected direct 1.7B ICL comparison using identical public Japanese short/normal/long fixtures, A current 46s and D short 4.55s legal private references, Auto/Japanese, one first-use + five warmed requests per config. All 12 MLX configs completed with zero sample failures; private JSONL/WAV at `/Volumes/Avalon/backups/operation-skuld/qwen3-tts/perf-matrix-20260926/mlx-results`. Supervised hard per-sample limit 110s; production timeout unchanged.
- Representative warmed p50/p95 (milliseconds): A Auto short MPS 4528/5197 vs MLX 3332/3465; A Auto normal 9770/9993 vs 5117/5211; A Auto long 16263/16999 vs 7939/8012. D Japanese short MPS 2716/2773 vs MLX 1229/1287; normal 7457/7638 vs 3199/3322; long 14558/14757 vs 5592/5811. Actual p50 audio durations were similar per matched fixture (A Japanese long 15520ms both; D Japanese normal MPS 7840ms vs MLX 8240ms). These are direct backend timings, not 9Router/WhatsApp acceptance.
- Memory/startup tradeoff: MLX Metal peak ~6.2–7.4 GiB for D, ~10.3–12.1 GiB for A; MPS driver peak ~5.0–6.1 GiB for D (A's earlier benchmark predates that metric). First MLX cold model startup ~23.7s, later per-process cached loads mostly ~1–2s. During PoC free-memory percentage fell transiently to ~16% while production TTS stayed healthy. The community backend uses a separate dependency stack and repeated reference preprocessing; maintenance and RAM headroom are material.
- The isolated `QwenMlxEngine` passed a real fixed short ICL smoke: WAV 24kHz/3.04s, model stage 3.458s, serialization 0.4ms. `engine_contract.py` retains the same timed-WAV API; source defaults to MPS, and an unknown/unfinished MLX configuration fails closed. No production MLX selection has occurred.
- Owner listening sheets are external and blank: `owner-quality-review.csv` (MPS) and `owner-mlx-quality-review.csv` (MLX). Kurisu similarity, Japanese naturalness, pronunciation and volume/rhythm cannot be inferred from latency. Do not promote MLX or short profile without explicit owner acceptance. Retain MPS/Auto/current profile as production candidate for now; QoS A/B/A/B already meets short HTTP latency goal with less memory/dependency risk.
- Rollback: no MLX runtime switch exists. If a future explicit MLX candidate is deployed, restore the current protected Qwen MPS service/plist checkpoint, rebootstrap LaunchAgent, verify health and real WhatsApp voice. Keep model weights and reference outside Git.
