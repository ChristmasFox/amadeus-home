# Phase 7 OpenClaw cache boundary benchmark — 2026-09-26

- Source commit `d84e8a0` rearranges only the OpenClaw Dockerfile: pinned base -> root ffmpeg OS dependency -> private glibc/Node runtime -> ffmpeg clean-env wrapper -> Amadeus dependency manifest/npm install -> plugin dist/skills -> pinned compiled compatibility patch -> node user. No new base image or secondary runtime.
- Controlled host BuildKit comparison used a minimal, allowlisted 8.2 MiB context outside Git at `/Volumes/Avalon/backups/operation-skuld/build-bench-20260926/context`. Logs stay external in the same protected directory. No CasaOS Compose switch or production container restart occurred. Test images are local `local/amadeus-openclaw-bench:*`, not release tags.
- Before reorder, a no-op comment change to `openclaw-voice-policy.mjs` rebuilt the ffmpeg apt step (22.6s), npm and glibc downstream; candidate wall time 39s. Initial matching baseline build wall time 41s, ffmpeg apt step 22.7s.
- After reorder, a matching no-op policy edit rebuilt only the final patch COPY/RUN; ffmpeg apt, private glibc and npm install were cached. Candidate wall time 3s. A plugin dist source-map whitespace edit built in 1s; ffmpeg, glibc and npm remained cached. BuildKit logs: `before-base.log`, `before-patch.log`, `after-base.log`, `after-patch.log`, `after-plugin.log`.
- The reordered image passed isolated network-none `scripts/test-openclaw-voice-image.sh --apply`: private Node workers, Longbridge native binding, ffmpeg MP3→Ogg/Opus child and ffprobe. `--dry-run --candidate --build-auto` still selects OpenClaw build and reuses Product Radar. Focused cache-order fixture, patch regression, architecture, secrets and diff checks passed.
- This proves local warm-cache behavior for these exact inputs; it does not replace the final immutable release build, CasaOS health, WhatsApp PTT acceptance or protected rollback checkpoint.
