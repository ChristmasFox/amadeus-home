# 2026-09-25 — Native auto-TTS-only candidate deployed

- Commit `a0ff768` pushed the config invariant: global OpenClaw `tools.deny=["tts"]` filters only the redundant Agent-facing core tool, while `profile=full`/owner `allow=["*"]` and boundary `tts.auto=inbound`, `mode=final` remain. Pinned matcher test verified sender allow cannot reintroduce the denied tool; config validation, shell syntax, diff and secrets scan passed.
- Explicit `deploy-openclaw.sh --apply --candidate --no-build --machine nyannyan` completed against the single Gateway. New protected external restore point `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925060705`; evidence directory `/Volumes/Avalon/backups/operation-skuld/deploy/amadeus-openclaw-20260925060705`. Same immutable `c64e29a` image, no release notification or version bump.
- Preflight, owner policy, Product Radar, Gateway health and NAS SSH smoke passed. Live config read-back confirms `deny=['tts']`, `auto=inbound`, `mode=final`; WhatsApp secondary healthy/linked/connected. Node restarted at ~14:07:15 Asia/Shanghai. No new voice after this restart at verification time.
- Await a new controlled WhatsApp voice note and owner handset count/playability. Expect zero explicit `tts` tool calls, one TTS request after final reply, one PTT send; if different, inspect only sanitized metadata and keep the release gate closed. Remaining A–L, quality and reboot acceptance still pending.
