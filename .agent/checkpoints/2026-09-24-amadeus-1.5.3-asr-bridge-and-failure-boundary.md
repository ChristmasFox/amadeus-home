# 2026-09-24 — 1.5.3 ASR protocol and WhatsApp failure boundary (source only)

- Official Alibaba Model Studio non-real-time ASR documentation confirms `qwen-audio-3.0-asr-flash` in the Beijing/Singapore regional model list, synchronous DashScope multimodal-generation API, Data URI input, 10 MB/5-minute model constraints, and nested `output.output.text`/`sentence.text` result. It does **not** document standard `/v1/audio/transcriptions` for this model. Reference: official `/zh/model-studio/non-realtime-speech-recognition-user-guide`, inspected on M204; no credential or user media was sent.
- Added a bounded, authenticated, deterministic multipart-to-DashScope adapter as a side process in the repository-managed 9Router image. 9Router still owns the `amadeus-asr` model alias and self-hosted STT connection. The adapter binds **container loopback** `127.0.0.1:20129`, converts Ogg/Opus or other supported inbound audio to 16 kHz mono WAV with ffmpeg, sends Data URI to the configured regional/workspace DashScope URL, normalizes `{text}`, maps `unsupported_audio`/`transcription_failed`/`provider_unavailable`/`auth_unavailable`/`timeout`, and caches at most 128 successful transcript/hash entries for five minutes. No audio is persisted or logged. Secrets/URL remain external env/readonly files; Compose template has secret mounts, but is **not applied**.
- The existing pinned WhatsApp preflight passes `null` when ASR fails and otherwise continues into a normal Agent turn. A source-controlled, fail-closed patch candidate now replies with a short text error to **admitted direct voice notes only** via the existing WhatsApp transport, without running Kurisu or inventing a transcript. It is wired into the deployment script after the external plugin backup boundary; it has not been applied to the live package. Group behavior and duplicate retries require real acceptance.
- Offline tests passed: ASR success/alias model request/response normalization, success retry cache, five failure classes and auth; pinned WhatsApp patch anchor/idempotence/JavaScript syntax and synthetic failure behavior; OpenClaw config schema; existing workflow/architecture/secrets. No Docker build, 9Router restart, OpenClaw restart, WhatsApp message, version bump or release occurred.
- Next: implement immutable 9Router build/deploy/checkpoint/rollback helper; secure external bridge token and DashScope key/region URL; real API and media smoke; then OpenClaw deploy and full WhatsApp acceptance. Production `kurisu-v1` reference still absent.
