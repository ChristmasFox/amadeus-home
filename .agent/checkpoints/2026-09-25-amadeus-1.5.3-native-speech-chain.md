# 2026-09-25 — Native speech chain through 9Router and OpenClaw candidate

- Deployed source-controlled ASR-only Node env-proxy opt-in in immutable 9Router image `local/9router:git-fed689894db5-20260925T050046Z`, checkpoint `/DATA/AppData/9router/backups/voice-1.5.3-deploy-20260925T050046Z`. It preserves the single live router, existing text routes and 401 on the published unauthenticated model listing. The ASR bridge child has `NODE_USE_ENV_PROXY=1`; the Next server does not. This is distinct from the concurrent app-level proxy-off checkpoint and does not prove resolution of the separate intermittent Codex/OpenAI proxy task.
- Before that image change, live bridge Node DNS returned `ENOTFOUND` for QwenAI and a native OpenClaw audio CLI request reached 9Router but returned provider-unavailable. With a one-off `NODE_USE_ENV_PROXY=1`, an unauthenticated QwenAI route reached HTTP 401. After deployment, a new synthetic Chinese WAV through live `amadeus-asr` returned HTTP 200 and nonempty 17-character transcript; bridge logged sanitized successful timing. No private WhatsApp media was used.
- The pinned OpenClaw native media transport blocked the internal `9router` DNS as SSRF before upload. A temporary config with provider-scoped `models.providers.openai.request.allowPrivateNetwork=true` advanced to 9Router. Committed source and a schema/contract fixture now scope that permission to the only OpenAI-compatible provider fixed at `http://9router:20128/v1`; browser/web SSRF policy was not loosened.
- Ran `scripts/deploy-openclaw.sh --apply --candidate --no-build` after its protected checkpoint `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925050221`. It reused the ffmpeg/libopus-capable immutable OpenClaw image `local/openclaw-amadeus:git-109d4590c6b4-20260925043803` and Product Radar; OpenClaw log explicitly reported config hot reload of `models.providers.openai.request`. The one Gateway remained healthy with WhatsApp linked/connected and Telegram running. Candidate mode kept `VERSION=1.5.2` and skipped release notification/maintenance.
- Synthetic real OpenClaw `capability audio transcribe --json` returned `ok=true`, one `audio.transcription` output with nonempty 13-character text in 3.73 seconds. Sanitized 9Router logs confirmed `amadeus-asr -> selfhosted-stt/qwen-audio-3.0-asr-flash` with bridge success; thus this was not a local Whisper substitution. Gateway `tts.status` showed `auto=inbound`, provider OpenAI; an authenticated one-shot Gateway `tts.speak` returned provider `openai`, MP3 `audio/mpeg` and nonempty inline audio (61,116 base64 characters) in 17.82 seconds. No reply was sent to WhatsApp by these CLI/RPC probes.
- **Not yet proven:** inbound WhatsApp voice-note acquisition, same-session transcript/tool loop, final PTT delivery, text-default typed replies, ASR/TTS text fallback, retry/idempotency, long-response behavior, audio quality judged by owner, and M204 reboot recovery. The 46-second anime-derived personal-use reference is an interim private voice; it is not evidence of the Goal's original-voice preference or third-party rights. The 1.5.3 version bump/release/main merge is not authorized by acceptance evidence yet.
