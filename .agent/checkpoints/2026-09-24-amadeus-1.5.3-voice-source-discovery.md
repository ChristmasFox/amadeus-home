# 2026-09-24 — Amadeus 1.5.3 voice discovery and source candidate

## Authority and safety

- Goal imported from `origin/goal/amadeus-1.5.3-voice-io` (`303ceb8`) onto the M204 `main` worktree; base is `b82bce0`, ahead of `origin/main` by one existing power-boundary commit. Goal was absent from `main` before this import.
- Read startup files, Git status/log and workflow plan. No runtime config, container, model, launchd, WhatsApp or version changes yet; `VERSION=1.5.2`.
- Live M204 guest is OrbStack `nyannyan`, not the historical `ubuntu` name. Live containers: 9Router `local/9router:0.5.81`, OpenClaw `local/openclaw-amadeus:git-3714ac906c55-20260924133738` healthy.
- Unauthenticated 9Router STT/TTS models and speech routes return 401. With the existing runtime OpenClaw key (never printed), `/v1/models/stt` lists Gemini STT models only, `/v1/models/tts` Gemini TTS only. The existing `amadeus-asr` is a Chat Combo; an authenticated synthetic WAV `/v1/audio/transcriptions` request returned 400 `Invalid model format`. No audio/provider credentials or private media were persisted.

## Source findings

- Pinned OpenClaw `2026.9.4` package docs and schema confirm `tools.media.models` with `provider: openai`, `baseUrl`, audio capability; `tts.auto: inbound`; OpenAI TTS custom `baseUrl`, model and `speakerVoice`; WhatsApp MP3 -> Ogg/Opus transcoding and pre-send text fallback. Local config candidate validates against the pinned schema, but no live voice-path acceptance has occurred.
- Current npm 9Router latest inspected: `0.5.86`; upstream source tag `v0.5.86` (`39e36d3`). Both live `0.5.81` and this version package selfhosted STT/TTS. STT handler does not expand Chat Combos; model aliases do resolve at `getModelInfo`. Self-hosted TTS adapter expects `model/voice` after provider prefix, calls an authenticated OpenAI-compatible `/v1/audio/speech`. Need prove exact DashScope ASR compatibility or implement a minimal protocol adapter before provisioning the `amadeus-asr` alias; no speculative live dashboard writes.
- Official Qwen3-TTS Base README documents reusable `create_voice_clone_prompt` and `generate_voice_clone`; candidate service uses these APIs with MPS, singleton, warmup before readiness and bounded inputs. No licensed `kurisu-v1` reference pair or TTS token exists at the operator-owned host paths. Native model inference has not been tested.

## Candidate changes and checks

- Added source for the native speech service, auth/readiness/bounds/rotating logs, deterministic WAV/MP3/Opus encoding, M204 LaunchAgent installer with explicit apply, pinned Python dependencies and mock-backed tests. Bundled `imageio-ffmpeg==0.6.0` encoder probe on M204 confirmed libopus and libmp3lame. No weights/reference media entered Git.
- Added OpenClaw example media/TTS configuration and workflow classification. Pinned OpenClaw config validation passed (six existing environment/plugin warnings in this local package context). Four service HTTP tests, shell/plist lint, workflow fixture and secrets scan passed at this checkpoint.
- The candidate configuration is **not deployed**. Existing typed WhatsApp behavior remains live. This is not a release, production voice, real ASR or WhatsApp acceptance claim.

## Next proof gates

1. Confirm official DashScope route/model and credential availability. Provision 9Router self-hosted STT connection + model alias and TTS connection + alias declaratively with backup; verify actual audio contracts and failure taxonomy.
2. Obtain operator-owned licensed `kurisu-v1` reference audio/transcript and runtime token outside Git. Install model and test MPS performance/quality, then source-controlled launchd service and reboot recovery.
3. Before runtime cutover, capture sanitized config/image/compose/launchd checkpoint; run direct ASR/TTS smokes and OpenClaw integration/failure tests. Then real WhatsApp matrix, latency/fallback, release bump, notes, commit and push.

## M204 engineering smoke after source checkpoint

- Created an external metadata-only pre-preparation checkpoint at `/Volumes/Avalon/backups/operation-skuld/voice-1.5.3-prep-20260924/metadata.json` (0600). The speech dir and LaunchAgent did not exist beforehand.
- Ran explicit `manage-qwen3-tts.sh --prepare-apply`: installed `qwen-tts==0.1.1`, Torch 2.14.0, soundfile 0.14.0, imageio-ffmpeg 0.6.0 into the external user venv; downloaded official Base model at pinned revision `fd4b254389122332181a7c3db7f27e918eec64e3` into the external model dir (about 4.2 GiB). `pip check` found no broken requirements.
- Using a **temporary Apple system synthesized English reference only for engineering**, actual MPS model load + cloned prompt warmup took 71.17 s; subsequent Chinese synthesis completed in 3.49 s and returned nonempty 24 kHz WAV. A temporary loopback-only service on 18793 showed `/healthz` 503 while loading and 200 when ready; unauthenticated `/v1/voices` 401, authorized voice inventory 200, and authenticated Chinese `/v1/audio/speech` MP3 200 with 45,836 bytes and a valid MP3 header. Stopped that temporary service and removed its reference audio/transcript/token. No production `kurisu-v1` asset exists, no launchd service installed, no 18792 listener, no CasaOS change, no WhatsApp message.
- Added idempotent, default-dry-run 9Router provisioning script with protected password/key files, guest online SQLite/compose/env/image checkpoint, connection drift checks, old Chat Combo retirement and stable aliases. Offline contract tests pass. This script is **not applied**: the exact official DashScope multipart route and credentials are not verified.

- Re-ran explicit `--prepare-apply` idempotently to create a fresh external `speech/tts.token` with mode 0600 and sufficient length (value suppressed). The operator-owned reference pair and DashScope/dashboard credentials remain missing; no production service or 9Router connection was created.
