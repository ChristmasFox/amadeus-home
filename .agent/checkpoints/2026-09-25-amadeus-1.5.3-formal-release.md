# 2026-09-25 — Amadeus 1.5.3 formal voice-lifecycle release

## Release and deploy

- `VERSION=1.5.3`; release notes passed `scripts/amadeus-version.sh check`.
- Release source commit: `9d02a89` (`release: Amadeus 1.5.3`). Prior feature/source commits: `7198206`, `8dc2383`, `1257b5e`.
- Formal OpenClaw image: `local/openclaw-amadeus:git-9d02a896b018-20260925120916`.
- Product Radar reused existing image `local/product-radar:git-d988000e1c5d-20260924130631`.
- CasaOS checkpoint: `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925120916`.
- Deploy evidence: `/Volumes/Avalon/backups/operation-skuld/deploy/amadeus-openclaw-20260925120916`.
- Release notification: sent. Owner outbox smoke: passed.

## Verification

- Formal deployment script's matching plugin build/typecheck/tests/secrets gates passed before image build and apply.
- Apply output: OpenClaw health passed, Product Radar health passed, NAS SSH read-only smoke passed.
- Post-apply read-only check: OpenClaw image matches release tag, container running/healthy, restart count 0; WhatsApp status reports linked/connected; core queue and WhatsApp typing markers each appear once.
- Corrected pre-apply checkpoint includes `/DATA/AppData/openclaw/config/npm/projects` as a directory (about 71,202,900 bytes), covering the external package patch.

## Warnings and acceptance still open

- `LOG_POLICY=warning`: verified external storage was unavailable.
- `POST_DEPLOY_MAINTENANCE=warning`; evidence says `GC=BLOCKED` because the external-storage gate failed. No cleanup was forced.
- `scripts/doctor.sh` reports 2 failures: media-organizer-adapter is absent; external storage / Immich media-boundary identity verification fails. Other observed OpenClaw/Product Radar/9Router/Immich health probes pass.
- After the explicit request to check direct voice delivery and group concurrency, the owner replied “正常”. Record this as qualitative owner confirmation that the lifecycle smoke had no reported issue; no message IDs, exact PTT/text counts or timestamps were supplied, so do not invent them.
- Remaining Amadeus 1.5.3 voice matrix A–L, ASR/TTS fallback, detailed voice quality/latency and reboot persistence remain open. Formal release is deployed, but do not claim all product acceptance gates passed.

## Consecutive-voice regression and hotfix candidate

After release, the owner reported two consecutive group voice notes where only one returned PTT. A bounded sanitized 12:18–12:19Z trace showed two Ogg-audio inbounds, two assistant final turns, and one media send. Pinned source analysis identified the core follow-up `routeReply()` path as bypassing normal final auto-TTS.

The ingress-FIFO fix is now deployed as a same-version candidate, commit `02fdf49`, image `local/openclaw-amadeus:git-02fdf490f8bb-20260925130109`; its full external checkpoint is `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925130109`. Candidate health, WhatsApp link state, marker counts and pre-apply mounted WhatsApp package backup were verified. The owner has not yet resent the two consecutive group voices after this hotfix, so this specific regression is not closed. See `.agent/checkpoints/2026-09-25-amadeus-1.5.3-consecutive-voice-tts-candidate.md`.

The prior qualitative “正常” feedback applied to an earlier lifecycle smoke; it is not treated as evidence for this newly reported consecutive-voice case. Remaining A–L, fallback, quality/latency and reboot acceptance also remain open.
