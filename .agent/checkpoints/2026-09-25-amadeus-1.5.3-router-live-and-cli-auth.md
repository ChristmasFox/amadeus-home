# 2026-09-25 — 9Router speech image live, direct bridge accepted

- The owner approved `qianwenaiapi.com` as the formal 1.5.3 ASR authority. After the prior automatic rollback and probe fix, the second explicit `scripts/deploy-9router-speech.sh --apply --allow-qwenai-upstream` passed. Canonical CasaOS Compose now runs immutable `local/9router:git-d882528fd59a-20260925T032728Z`; protected rollback checkpoint: `/DATA/AppData/9router/backups/voice-1.5.3-deploy-20260925T032728Z`, with old image archive on Avalon. OpenClaw remained on its previous healthy image/config.
- Post-apply verification: Mac published 9Router `/api/health=200`, unauthenticated `/v1/models=401`; container-local 9Router health=200, ASR bridge health=200, guest-to-M204 TTS health=200. Container-local `/v1/models` may return 200 by upstream local-peer policy, so the API-key assertion must use the published ingress.
- A 2.71-second Mac-generated Chinese WAV (not user media) posted with the protected bridge Bearer token directly to the **live** container-loopback ASR bridge returned HTTP 200, normalized nonempty `{text}` of 12 characters in approximately 8.1 seconds. Neither key, audio payload nor transcript was logged or committed. This proves bridge -> QwenAI upstream, not yet 9Router `amadeus-asr` alias or WhatsApp input.
- Discovered upstream's intended local CLI admin-token path in its pinned primary source: token derives from protected machine-id, random cli-secret and fixed `9r-cli-auth` salt. A read-only CLI-token request to `/api/providers` returned 200 without exposing token or rotating dashboard password. The source-controlled alias provisioning script now uses this protected local CLI token by default; optional password-file mode remains. Offline auth fixture tests passed.
- No provider connection/alias writes have yet occurred in this checkpoint. Next: commit the CLI-auth source, take its online DB checkpoint and provision Self-hosted STT/TTS aliases, restart once for the pinned STT alias cache, then direct real 9Router ASR/TTS and OpenClaw/WhatsApp acceptance.
