# 2026-09-25 — Run-scoped voice Skill injection candidate live

- Source commit `1c98642` follows a real attempt that produced Japanese-only final text and one PTT because the Agent had not selected the voice-reply Skill. The same conflict in SOUL has been resolved. Build-auto rebuilt only OpenClaw with the bounded native hook, then explicit `deploy-openclaw.sh --apply --candidate --build-auto --machine nyannyan` switched the single runtime.
- New immutable ARM64 image `local/openclaw-amadeus:git-1c986422c6fe-20260925073459`; protected external restore `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925073459`, evidence `/Volumes/Avalon/backups/operation-skuld/deploy/amadeus-openclaw-20260925073459`. Candidate mode leaves version 1.5.2 and skips release notification.
- Preflight, health, Amadeus plugin registration, skill eligibility and WhatsApp secondary linked/connected passed. Plugin loads the canonical skill from `api.rootDir/skills/voice-reply/SKILL.md`; run tracker is in memory and bounded (runId only, max 128, TTL 10m). Architecture guard admits only this channel+audio+run-scoped prompt module; all type/test/architecture/secrets checks pass.
- No fresh real voice inbound since the restart at ~15:35. Await controlled user voice; inspect final output markers only, TTS count, send logs, handset count/order and summary fidelity. Explicit Chinese output, typed path and failures still need acceptance. Do not call feature complete or release 1.5.3.
