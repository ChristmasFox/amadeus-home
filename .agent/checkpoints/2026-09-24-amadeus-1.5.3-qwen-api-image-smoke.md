# 2026-09-24 — Existing Qwen API and ARM64 bridge candidate

- Existing live 9Router active custom `Qwen` provider holds a protected API key and a `https://*.qianwenaiapi.com/compatible-mode/v1` Responses endpoint. The `amadeus-asr` Combo is a chat grouping, not `/v1/audio/transcriptions` routing. No key or full private endpoint was printed or committed.
- The same key was used only inside M204 guest for two minimal synthetic (Mac-generated 2.71-second WAV) requests to the platform's Qwen-Audio-3.0-ASR-Flash multimodal-generation path. Both returned HTTP 200 and nonempty transcript. Response schema includes top-level `text` and `output.text`, not only Alibaba's nested `output.output.text`; text contents were suppressed. No user voice note or private profile was sent.
- Source adapter now has exact QwenAI host allowlist alongside Alibaba workspace allowlist and normalizes both output schemas. The existing credential was copied to protected guest `/DATA/AppData/9router/secrets/asr-upstream-api-key` (uid 1000, 0600) and the endpoint added to external `9router.env` after checkpoint `/DATA/AppData/9router/backups/voice-qwen-source-20260924T200517Z`. Live 9Router container/DB aliases were not changed.
- Deployment source rejects this QwenAI authority unless `--allow-qwenai-upstream` is explicit. A fail-closed `--apply` probe without that flag exited 1 before export/build/Compose, leaving `local/9router:0.5.81` live.
- Built ARM64 candidate from committed source `8a47b8508aaf`: `local/9router:git-8a47b8508aaf-20260924T200816Z`, guest digest `sha256:9922dd4f303b14e9aa7c2473c66044c9d32c44e755cdfde214a3fa1262985449`. A fresh network-none isolated image test passed bridge auth, ffmpeg conversion, TTS alias, protected API key creation and ASR alias after process restart; cloud failure was intentionally `provider_unavailable`. No live container switched and no actual 9Router->QwenAI route was exercised yet.
- This is a supplier-authority change relative to the original Goal's Alibaba Model Studio/DashScope, not a claim that the two services are identical. Request explicit operator decision before production cutover. `VERSION=1.5.2`; WhatsApp acceptance and release remain open.
