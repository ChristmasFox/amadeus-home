# 2026-09-25 — Live 9Router STT/TTS aliases and direct speech acceptance

- Applied source-controlled `scripts/provision-9router-speech.py --apply` using the upstream's protected local CLI token, without resetting or printing the dashboard password. It took an online SQLite/Compose/env/image checkpoint first: `/DATA/AppData/9router/backups/voice-1.5.3-20260925T040813Z`. The prior DB passed `PRAGMA integrity_check=ok`, had the old `amadeus-asr` Chat Combo and no Self-hosted STT/TTS connections.
- Created one Self-hosted STT connection to container-loopback bridge and one Self-hosted TTS connection to M204 native service, retired the old Chat Combo, set `amadeus-asr -> selfhosted-stt/qwen-audio-3.0-asr-flash` and `amadeus-tts -> selfhosted-tts/qwen3-tts-1.7b/kurisu-v1`, then restarted 9Router once for its pinned STT alias cache. Post-restart health/API-key gate passed. Live SQLite integrity remains `ok`; old combo 0, STT/TTS connections 1 each. API returned both aliases exactly as intended; no credential or user audio logged.
- Direct authenticated **live 9Router** `/v1/audio/transcriptions` with a short Mac-generated Chinese WAV and `model=amadeus-asr` returned HTTP 200, nonempty 12-character transcript (0.79 s; likely five-minute cache after earlier bridge test). A distinct uncached synthetic phrase returned HTTP 200 and nonempty 14-character transcript in 1.52 s. Direct `/v1/audio/speech` with `model=amadeus-tts` returned HTTP 200, `audio/mp3`, 34,316 bytes with a valid MP3 header in 13.19 s. No synthetic audio was retained. These prove the standard 9Router logical routes, not WhatsApp delivery or subjective voice quality.
- Canonical Compose and live container still use immutable `local/9router:git-d882528fd59a-20260925T032728Z`, digest `sha256:48578104bc6dbb747de5c6f3f181d288ac95cc74f67396d1ee4f70090215ce87`. Published unauthenticated `/v1/models` remains 401. OpenClaw still uses its old healthy image/config; `VERSION=1.5.2`.
- Pinned OpenClaw container currently has **no `ffmpeg` binary**, while its WhatsApp voice-note sender needs MP3 -> 48 kHz Ogg/Opus conversion. The repository OpenClaw Dockerfile now installs Debian ffmpeg with a libopus encoder assertion. This source change requires a new immutable OpenClaw image before voice replies can be claimed. OpenClaw/WhatsApp acceptance remains pending.
