# 2026-09-25 — Verified-audio scoped Skill injection (source only)

- Owner received one Japanese PTT but no Chinese text. Read-only session event at 15:10 showed Japanese-only final text, no `中文：` prefix or `[[tts:text]]`; one media send. The model did not choose the loaded voice-reply Skill. A later fresh non-delivered CLI input described as WhatsApp voice also omitted the contract, even after the earlier SOUL conflict was removed and Skill description strengthened.
- To reliably carry the user's voice-format preference without placing workflow in SOUL/AGENTS or adding a keyword router, `plugins/amadeus/src/voice-reply-prompt.ts` uses the pinned native `message_received` event's trusted `media`/`originalMedia` facts and exact runId, then `before_prompt_build` appends the *single authoritative voice-reply Skill body* only to that WhatsApp audio run. It does not copy inbound prompt/content, does not run on typed/Telegram turns, retains IDs for max 10 minutes and at most 128 entries, and logs no content. No new Agent, sender, or planner.
- Architecture check now allows this one scoped file only and asserts audio, WhatsApp, run-id, TTL/cap, and canonical Skill source anchors. Amadeus typecheck and all 35 tests, architecture/secrets/diff pass, but source is not yet committed/built/deployed.
- Pending: explicit release candidate build/apply with new external checkpoint; real owner voice verifies one Japanese PTT plus one Chinese text, visible summary fidelity/order, typed path unaffected, and explicit Chinese output behavior. Do not count prior PTT-only attempt as bilingual success.
