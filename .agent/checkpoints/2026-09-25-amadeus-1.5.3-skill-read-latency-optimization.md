# 2026-09-25 — Remove redundant Skill read from verified-audio turns

- Owner confirms the Chinese text summary reached the phone for the 15:51:48 WhatsApp group voice turn. OpenClaw's final model metadata contains one Chinese visible sentence and one Japanese `[[tts:text]]` block; WhatsApp logs one group PTT send. This is one user-confirmed group dual-format observation, not the required direct-DM/A–L matrix.
- Measured turn: ASR 405 ms; model call 1 15.522 s then assistant `read` tool call to reload the already-injected voice-reply Skill; model call 2 10.685 s; Qwen3-TTS/MP3 34.121 s; delivery tail 1.91 s; total ingress-to-PTT 63.566 s. The exact `assistantTexts` metadata showed both languages plus one TTS text block; no contents persisted.
- Source-only optimization in `plugins/amadeus/src/voice-reply-prompt.ts` now explicitly tells the same verified WhatsApp audio run that the entire Skill body is already included and not to call `read` just to retrieve it again. It does not remove domain-tool access or change planner behavior. Amadeus typecheck/35 tests, architecture, secrets and diff checks pass.
- **Not yet applied.** Build/candidate apply requires a fresh external checkpoint. Then send another controlled message and compare agent call count/first latency, TTS duration, text/PPT logs and owner phone receipt. Even if the redundant model round disappears, the measured ~34-second MPS TTS remains a distinct optimization target.
