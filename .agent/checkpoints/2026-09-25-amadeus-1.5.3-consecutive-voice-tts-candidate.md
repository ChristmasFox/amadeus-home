# 2026-09-25 — Consecutive voice ingress-FIFO hotfix candidate applied

## Candidate apply

- Source commit: `02fdf49` (`fix(whatsapp): serialize consecutive voice turns before dispatch`).
- Version remains `1.5.3`; this is a same-version pre-acceptance candidate, not a new release.
- OpenClaw image: `local/openclaw-amadeus:git-02fdf490f8bb-20260925130109`.
- Product Radar reused `local/product-radar:git-d988000e1c5d-20260924130631`.
- External checkpoint: `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925130109`.
- Deployment evidence: `/Volumes/Avalon/backups/operation-skuld/deploy/amadeus-openclaw-20260925130109`.
- Read-only checkpoint verification: `/DATA/AppData/openclaw/config/npm/projects` captured as a directory, about 71,202,900 bytes.

## Verification

- `pnpm test`, `pnpm typecheck`, `pnpm check:architecture`, and `pnpm check:secrets` passed before candidate build/apply.
- Exact live pinned `@openclaw/whatsapp` 2026.9.4 monitor source accepted the ingress-queue v2 patch; `node --check` passed and re-apply was idempotent. The deployment stdin invocation applied both markers.
- After apply: OpenClaw running/healthy, restart count 0; WhatsApp linked/connected. Core queue, voice lifecycle v1, and voice ingress FIFO v2 markers each appear once.
- One OpenClaw runtime only; no parallel/shadow gateway.

## Retest required

The owner reported two consecutive voice notes in a group where one got PTT and one only text. This candidate holds later same-session WhatsApp arrivals at `processForRoute` while a voice lease is active, then runs each through the normal inbound dispatch and automatic TTS pipeline. A queued voice obtains its own composing lease. It avoids the core followup `routeReply()` path that bypasses auto-TTS.

Please resend **two consecutive voice notes in the same group**. Expected for each, in order: one Japanese PTT plus one concise Chinese summary; composing remains through both sends. Confirm both notes receive their own PTT and that no later text/voice steers or overlaps the active response. No post-hotfix handset evidence yet; do not bump to 1.5.4 or call this regression closed until the owner retest.
