# 2026-09-25 — Amadeus 1.5.6 formal release

## Acceptance and scope

- Owner confirmed the same-version 1.5.5 hotfix candidate worked (“现在好了”) and asked to commit, push and formally deploy.
- The accepted voice contract is Japanese PTT + matching Japanese kanji/kana line + concise Chinese summary. Typed-only behavior is unchanged.
- Root cause addressed in the candidate: WhatsApp 2026.9.4 disables `message_received` plugin hooks by default and suppresses the core fallback. The Amadeus voice Skill now binds to the existing active WhatsApp audio lease; a delivery guard strips audio when the synthesized source has no Japanese kana and returns a text notice instead of an obvious Chinese PTT.

## Source and release gates

- `./scripts/amadeus-version.sh bump patch` advanced `VERSION` from 1.5.5 to 1.5.6; `RELEASE_NOTES.md` contains only this release.
- Full `pnpm test`, `pnpm typecheck`, `pnpm check:architecture`, `pnpm check:secrets`, version check, voice lifecycle/parser tests and `git diff --check` passed.
- Release commit `bdcc07c` was pushed to `work/amadeus-1.5.3-voice-io`; formal apply used `./scripts/deploy-openclaw.sh --apply --build-auto` after a successful dry-run.

## Formal deployment result

- UTC deploy stamp: `20260925183945` (Asia/Shanghai: 2026-09-26 02:39:45).
- OpenClaw image: `local/openclaw-amadeus:git-bdcc07c43afc-20260925183945`.
- Product Radar image reused: `local/product-radar:git-d988000e1c5d-20260924130631`.
- Rollback checkpoint: `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925183945`. Its manifest confirms `/DATA/AppData/openclaw/config/npm/projects` was backed up as a directory (71,207,880 bytes).
- Deployment evidence: `/Volumes/Avalon/backups/operation-skuld/deploy/amadeus-openclaw-20260925183945`.
- OpenClaw/Product Radar health and NAS SSH read-only smoke passed; OpenClaw is running/healthy with restart count 0, WhatsApp is linked/connected/healthy, and there is one OpenClaw runtime.
- Live image contains the Japanese Skill, active-lease injection and audio fail-closed guard. The four lifecycle/FIFO/visible-text/audio-guard markers occur exactly once; the monitor passes `node --check`.
- Formal release outbox status is `sent`; `OWNER_OUTBOX_SMOKE=passed`.
- Owner had already accepted the identical voice behavior on the 1.5.5 candidate before the formal apply.

## Non-blocking post-deploy warnings

- The verified external-storage gate was unavailable. `log-policy.log` records `LOG_POLICY=blocked; verified external storage is unavailable`; `storage-maintenance.log` records `REASON=verified external storage gate failed`. Docker log policy and post-deploy cleanup/GC were not applied; no gate was bypassed.
- The optional media organizer adapter was absent, so its network smoke was skipped. This does not affect the voice release; media capability acceptance remains separate.
- Storage follow-up remains open in `.agent/tasks/2026-09-26-post-deploy-storage-gate.md`.
