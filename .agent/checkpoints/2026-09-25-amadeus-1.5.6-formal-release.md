# 2026-09-25 — Amadeus 1.5.6 formal release

## Candidate acceptance and authorization

- User confirmed the Japanese-audio injection hotfix candidate works (“现在好了”) and explicitly requested commit/push/formal deployment.
- Root cause: pinned WhatsApp 2026.9.4 defaults `channels.whatsapp.pluginHooks.messageReceived` to false; WhatsApp also suppresses the core fallback, so the first two trackers never injected the Japanese voice Skill. The hotfix uses the trusted active WhatsApp voice lease instead and provides an outbound fail-closed guard.
- Accepted behavior on candidate `local/openclaw-amadeus:git-12df1ae6b2e1-20260925182336`: voice replies produce Japanese PTT + matching Japanese kanji/kana line + Chinese summary; typed-only path is unchanged. A sanitized post-candidate trace showed Japanese kana and Chinese label on three recent assistant voice finals; no original text was stored here.
- `./scripts/amadeus-version.sh bump patch` advanced 1.5.5 to 1.5.6. `RELEASE_NOTES.md` has only 1.5.6 content.

## Release gates and apply

- Before deployment, run `pnpm test`, `pnpm typecheck`, `pnpm check:architecture`, `pnpm check:secrets`, `./scripts/amadeus-version.sh check`, voice-specific tests and `git diff --check`.
- Commit/push release source; dry-run and then formally apply using `./scripts/deploy-openclaw.sh --apply --build-auto`.
- Record immutable image tags, external rollback checkpoint, deploy evidence, OpenClaw/WhatsApp health, owner release notice, and post-deploy warnings below.
- Do not claim 1.5.6 is live until formal apply and runtime health are verified.
