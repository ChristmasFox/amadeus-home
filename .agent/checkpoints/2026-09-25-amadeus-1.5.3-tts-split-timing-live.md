# 2026-09-25 — M204 TTS stage timing deployed, short-speech test pending

- Protected pre-apply external restore point: `/Volumes/Avalon/backups/operation-skuld/qwen3-tts/timing-20260925T082939Z` (old service.py, LaunchAgent plist and sanitized status; no voice asset/token). Source/test already committed at `83e8e8f`.
- `infra/macos/manage-qwen3-tts.sh --apply` copied the Git service and plist, but initial `launchctl bootstrap` returned `Input/output error`; service was down until explicit `launchctl bootstrap gui/501 ...` succeeded. `launchctl enable` then set persistent enablement; `manage-qwen3-tts.sh --status` shows running, `/healthz=200`, and live source SHA exactly matches Git. This recovery is recorded so rollback/restart path is clear.
- Direct authenticated local synthetic Japanese MP3 smoke returned HTTP 200/valid MP3. New sanitized log splits `engine_ms`, `encode_ms`, `audio_ms`, `total_ms` and input-length bucket without text. Two same-sentence attempts produced 15.840s/0.337s with 4.72s audio and 39.733s/0.205s with 5.04s audio. Variance is unexpectedly high; encoding is negligible; more paired samples and stage-level GPU profiling needed before changing model options.
- The OpenClaw voice Skill now targets one Japanese short sentence <=50 codepoints (without dropping essential warnings). Candidate image `local/openclaw-amadeus:git-4861970f0338-20260925083938` is live and healthy, but after its ~16:40 restart only typed text was observed; no real voice checks the length target yet. Next: owner sends a controlled voice; compare speaker language, exact TTS `engine_ms`/audio duration, PTT count and Chinese text. No release claim.
