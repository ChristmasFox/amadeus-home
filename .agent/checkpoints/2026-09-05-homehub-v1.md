# Checkpoint: HomeHub V1

日期：2026-09-05（Asia/Shanghai）
父提交：a37b2cd（docs: defer WhatsApp integration due to commercial API requirements）
完成提交：ba1d556（feat: add HomeHub v1 runtime）

## 目标

恢复并完成上一个 session 中断的 HomeHub V1 实现：在现有 Mastra/PUBG runtime 中加入
平台无关的 HomeHub domain、服务健康诊断、确认式操作、审计/上下文、媒体整理安全流程，
并接入 HTTP 与 PUBG/HomeHub 路由分流。

## 本阶段完成

- 新增 `packages/homehub-domain`，包含 schema、服务 registry、主机采集、诊断、操作引擎、
  上下文管理和审计日志。
- 新增 `apps/agent-runtime/src/homehub` entry 与媒体整理操作器，以及 HomeHub runtime facade。
- 增加 `/homehub/health`、`/homehub/route`、`/homehub/query` 和 Telegram polling endpoint。
- `/v3/query` 在进入 PUBG workflow 前分流 HomeHub 请求。
- 高风险操作需要显式确认；ActionRequest 默认字段在边界规范化；媒体整理要求明确下载项目，
  预览后确认，备份后逐项移动，限制 source/destination roots，拒绝覆盖已有目标。
- 修复查询分类将“服务器怎么样”误判为诊断、授权检查顺序错误和电影年份被识别为动漫集数的问题。
- 更新 agent runtime README、项目状态、当前任务和后续 smoke-test task。

## 验证

- `pnpm typecheck`：通过。
- `pnpm build`：通过。
- `pnpm test`：92 tests，91 pass，1 skip（既有外部 fixture 缺失）。
- HomeHub targeted test：9/9 通过，包含媒体入口确认、预览/备份/移动及越界拒绝。
- 目标环境 CasaOS 部署和真实 Telegram/KOOK 入站烟测：未执行，记录在 `.agent/tasks/homehub-runtime-smoke.md`。

## 恢复与回滚

本 checkpoint 对应提交完成后，可回滚到父提交 `a37b2cd`；不修改运行中的容器或 CasaOS compose，
不执行公网 push。运行时部署必须在审阅备份后使用显式 apply 流程。
