# 2026-09-25 — Node worker-compatible ffmpeg wrapper image fixture

- Source commit `c64e29a` replaces the failed explicit-loader approach. The Node launcher again exports private Longbridge glibc so `process.execPath` stays `node.glibc` for OpenClaw's SQLite workers. The immutable image instead puts a Node-interpreted clean-env wrapper at `/usr/bin/ffmpeg` and `/usr/bin/ffprobe`; it removes `LD_LIBRARY_PATH` only before executing the Debian media binary and passes through stdio/exit status.
- Host BuildKit ARM64 image `local/openclaw-amadeus:git-c64e29ad688d-20260925053214` built successfully; the Docker build's child ffmpeg version smoke exited 0. The first standalone image fixture attempt exposed a **test-script quoting error** in its Node worker check, not an image/runtime failure. The fixture script was corrected before a second isolated run.
- Final `scripts/test-openclaw-voice-image.sh --apply --image ...` used a network-none disposable container with no credentials or user data and passed all four checks: private Node worker self-spawn, Longbridge native binding load, synthetic MP3 -> Ogg/Opus ffmpeg child conversion and ffprobe child. The old live image was independently shown to fail this fixture with contaminated child library path.
- The new image has **not yet** replaced the sole live OpenClaw candidate in this checkpoint. The previous candidate apply of the explicit-loader image failed during preflight before Compose mutation; its protected rollback checkpoint remains `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925052705`. Next: commit the corrected fixture, transfer this immutable image to OrbStack, apply one candidate runtime with a new checkpoint, then obtain an owner voice-note resend and actual WhatsApp PTT receipt.
