# 2026-09-25 — Short Japanese spoken-reply policy (source only)

- After the no-read change, a direct DM voice sample produced one Agent completion and a Japanese speech block plus Chinese summary; the spoken block had 68 codepoints. Qwen host timing split showed `engine_ms=35162` with only `encode_ms=205`, proving encoding is not the main TTS bottleneck.
- Source Skill now requests a single natural Japanese sentence targeted at <=50 codepoints, retaining the direct answer and essential/safety caveat; the Chinese sentence carries additional key facts. This is a soft LLM instruction, not deterministic truncation. No hard model max-token or voice-model/quality setting has been altered.
- Voice parser/Skill test, architecture, secrets and diff checks pass. Candidate build/apply and Japanese naturalness/quality measurement are pending; the estimate that less speech may be faster remains unverified. Preserve user's language policy: typed Chinese default; audio Japanese PTT + Chinese summary.
