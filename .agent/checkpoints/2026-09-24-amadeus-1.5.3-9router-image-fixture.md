# 2026-09-24 — 9Router voice candidate image and isolated acceptance

- Current goal branch: `work/amadeus-1.5.3-voice-io`, source commit before fixture `510c256631ea`. Production still runs `local/9router:0.5.81` and the prior healthy OpenClaw image; `VERSION=1.5.2`.
- Exported the exact old 9Router image before staging: protected external artifact `/Volumes/Avalon/backups/operation-skuld/9router/9router-0.5.81-20260924T190450Z.tar.gz`, SHA-256 `2250039f3ae92520842712bbdceb3e80ec9095b4ddc12f24e1856c6f423846c2`. No data or key was printed or committed.
- Host BuildKit ARM64 image build passed from source commit: `local/9router:git-510c256631ea-20260924T190513Z`, guest image digest `sha256:c6f6b6dec75357a0d0d29517281b25a48fb7df27324488b021f6fe8ee4531000`. Loaded **without** changing canonical Compose/container.
- First isolated fixture boot exposed a real permission mismatch: upstream `/entrypoint.sh` drops privileges to uid 1000 (`node`), so root-owned 0600 mounted ASR key was unreadable. Source secret preflight and fixture generation now require uid 1000/mode 0600. This is a verified fix, not a claimed production rollout.
- Final `scripts/test-9router-speech-image.sh --apply` with synthetic fixture keys, isolated data and `--network none` passed: packaged 9Router health, bridge health, Bearer 401, actual ffmpeg WAV conversion, and normalized `provider_unavailable` when cloud egress is intentionally disabled. Fresh fixture DB does not enforce the production API-key setting; the apply script must verify the preserved live `/v1/models` 401. Temporary fixture container/data were removed.
- Created a high-entropy bridge Bearer key via explicit `scripts/prepare-9router-speech-secrets.sh --apply`, with a protected host copy and guest `/DATA/AppData/9router/secrets/asr-bridge-key` uid 1000/mode 0600; key value suppressed. DashScope key, matching regional/workspace URL and dashboard-password file are still absent. The operator-owned production reference audio/transcript is still absent; its directory exists with mode 0700.
- No canonical 9Router/OpenClaw Compose restart, provider alias mutation, cloud inference or WhatsApp message occurred. Direct ASR/TTS, production voice, rollback after live cutover and 1.5.3 release remain unverified.

## Capability planning answers

- Intent: automatic WhatsApp DM voice-note transcription and inbound-modality reply; no Telegram-specific implementation or explicit speech tool. Trusted WhatsApp admission/session stays in OpenClaw; no new identity or confirmation bypass.
- Domain/tool ownership: no new deterministic business domain or native plugin tool. OpenClaw native media/TTS owns lifecycle; 9Router owns logical model aliases; bridge only adapts byte/JSON protocol. No SOUL/AGENTS workflow or keyword routing.
- Contract/evidence: `{text}` or structured failure categories with no invented transcript. Audio/time constraints are byte, duration-equivalent and timeout bounds; no market-like query time semantics. Evidence is sanitized route/model/status/latency, never private audio/content.
- Side effects: Qwen model and voice profile remain external, bridge keys are external; bounded transcript-only in-memory retry cache; no new sender/outbox. Protected old-image + online SQLite/Compose/env/secret checkpoints support rollback.
- Validation: Node ASR failure fixtures, Python TTS tests, pinned config schema, image fixture, workflow/architecture/secrets and eventual direct 9Router + real WhatsApp matrix. No second runtime or notification fallback.
