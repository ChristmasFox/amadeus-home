# 2026-09-25 — Native OpenClaw audio SSRF and scoped QwenAI bridge egress

- Candidate OpenClaw single runtime was deployed with external checkpoint `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925043803`, immutable `local/openclaw-amadeus:git-109d4590c6b4-20260925043803` and Product Radar image reuse. It remained `VERSION=1.5.2`, sent **no release notification**, and did not create a second Agent. OpenClaw health, both channels' connection state, pinned WhatsApp ASR failure patch, ffmpeg/libopus and actual `tts.auto=inbound` config were confirmed. Real WhatsApp modality is still unverified.
- A synthetic `openclaw capability audio transcribe` request initially failed **before upload** with `SsrFBlockedError` because media transport denies a private `9router` DNS target by default. Pinned `2026.9.4` schema supports `models.providers.openai.request.allowPrivateNetwork`. A temporary runtime-only config with this flag validated and advanced the request to 9Router, proving the narrow policy is needed; no broad browser/web SSRF relaxation was used. Source now scopes the flag to the single OpenAI-compatible provider whose base URL is fixed `http://9router:20128/v1`, with a schema/contract test.
- That temporary call then returned 9Router 503 `provider_unavailable`. Live 9Router data showed one self-hosted STT account locked for the model after a bridge upstream failure. Container Node 22 `fetch` resolved the QwenAI host as `ENOTFOUND`, whereas an unauthenticated request from the guest Python path reached the service and returned 401. With `NODE_USE_ENV_PROXY=1` in a one-off Node process using the already-persisted host proxy environment, the same no-key upstream route returned 401. No key or user audio was exposed.
- Concurrent source commit `a71f881` had independently restored M204 9Router process-start HTTP(S)_PROXY and NO_PROXY, with its own checkpoint. This voice patch scopes `NODE_USE_ENV_PROXY=1` to the **ASR bridge child only**; the packaged 9Router server retains its own outbound proxy policy. It is not a fix/claim for the separate intermittent Codex/OpenAI proxy/DNS issue in `.agent/tasks/2026-09-25-9router-dns-proxy-failclosed.md`.
- Source image fixture now asserts only the bridge child has the env-proxy opt-in. Targeted Node, OpenClaw config-schema, architecture, workflow and secrets checks passed. The source changes are not yet deployed. Next: immutable 9Router build/apply with checkpoint and live ASR recovery, then single OpenClaw candidate config apply without image rebuild and real WhatsApp acceptance.
