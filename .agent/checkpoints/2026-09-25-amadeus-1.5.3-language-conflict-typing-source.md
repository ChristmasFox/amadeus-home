# 2026-09-25 — One PTT, Chinese preference conflict, typing source candidate

- User reports a single phone reply for the latest voice note, but it remained Chinese and the WhatsApp three-dot typing indicator was not visible. One owner direct Opus inbound at 14:22:11, one 9Router ASR success, one TTS at 14:22:17 and one media-send at 14:22:56. The previous Agent side-send duplication is not observed on this trial; handset playability/language quality still require a post-language-fix test.
- Source/runtime SOUL now says Japanese default. However live `USER.md` still has the exact stale `Prefer Simplified Chinese.` preference; it differs from the Git seed and has additional user content. An explicit one-line migration script (dry-run by default, exact anchor, atomic regular-file replacement) and fixture preserve all other lines and refuse symlinks/drift; Git USER seed now specifies Japanese default with Chinese only on explicit request. No private extra USER lines are copied into Git.
- Pinned OpenClaw 2026.9.4 docs/source: DM typing defaults to instant and refreshes while a run is active; WhatsApp `composing` presence is ephemeral/best effort and not guaranteed on every client. TTS occurred after model response and lasted roughly 40 seconds; whether presence persisted through synthesis is unproven. Source explicitly sets `agents.defaults.typingMode=instant`, `typingIntervalSeconds=3` to improve visibility without claiming certainty. No custom sender or channel keyword behavior is added.
- Focused speech config, migration guard/idempotence, workspace and architecture checks, secrets scan and diff check passed. Source not yet applied; take a fresh external checkpoint, candidate apply, guarded USER one-line patch, restart, then real Japanese reply and client indicator acceptance. Do not claim 1.5.3 release.
