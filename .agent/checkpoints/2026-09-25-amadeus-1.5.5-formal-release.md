# 2026-09-25 — Amadeus 1.5.5 formal release

## User authorization and scope

- User asked to fix the language policy: voice-output audio must always be Japanese, even when the user requests Chinese speech; ordinary text-message behavior must remain unchanged.
- User then explicitly requested “正式发布”. Per that direct request, this release proceeds without a separate same-version candidate step; do not misrepresent this as pre-deployment handset acceptance.
- Voice replies retain the existing visible Chinese summary and Japanese kanji/kana line exactly synchronized with the Japanese PTT. Typed-only behavior, including explicit language requests, is unchanged.

## Release source and gates

- `./scripts/amadeus-version.sh bump patch` advanced `VERSION` from 1.5.4 to 1.5.5; `RELEASE_NOTES.md` contains only this release.
- Passed before apply: full `pnpm test`, `pnpm typecheck`, `pnpm check:architecture`, `pnpm check:secrets`, `./scripts/amadeus-version.sh check`, `node scripts/test-openclaw-bilingual-voice.mjs`, `git diff --check`.
- Source commit/push and dry-run must complete before formal apply.
- Applied with `./scripts/deploy-openclaw.sh --apply --build-auto` after dry-run; OpenClaw-only rebuild, Product Radar reused.
- UTC deploy stamp: `20260925164645`; Asia/Shanghai local time: September 26, 2026 00:46:45.
- OpenClaw image: `local/openclaw-amadeus:git-07918b6aea33-20260925164645`; Product Radar image reused: `local/product-radar:git-d988000e1c5d-20260924130631`.
- CasaOS rollback checkpoint: `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925164645`.
- External deploy evidence: `/Volumes/Avalon/backups/operation-skuld/deploy/amadeus-openclaw-20260925164645`.
- OpenClaw/Product Radar health checks passed. Post-apply OpenClaw is healthy, restart count 0; WhatsApp linked/connected/healthy; one OpenClaw runtime. Formal owner notification outbox status is `sent`.
- Post-deploy warnings: Docker log policy was blocked because verified external storage was unavailable; storage maintenance reported `verified external storage gate failed`. Neither log policy nor GC was applied. Evidence files are `log-policy.log` and `storage-maintenance.log` in the deploy evidence folder; follow-up is `.agent/tasks/2026-09-26-post-deploy-storage-gate.md`.
- The optional media organizer adapter was absent, so its network smoke was skipped; separate media acceptance remains pending.

## Owner handset retest pending

- No post-release mobile message was sent by the agent. Please test: voice input explicitly requests Chinese speech, but PTT remains Japanese and matches the visible Japanese kanji/kana line, with the existing Chinese summary; typed-only message language behavior remains unchanged.
