# 2026-09-25 — Three playable PTTs and redundant Agent tool root cause

- Owner explicitly reports three WhatsApp voice replies received, all playable, for the fresh 13:55:15 note. This closes handset receipt/playability for that trial, but does not imply one-reply idempotency, correct transcription, subjective voice quality, or full A–L acceptance.
- A read-only query of the live OpenClaw SQLite transcript event **metadata** (no content copied) showed one `user` event, assistant `toolUse` with `tts` twice at 13:55:22 and 13:55:52, two `toolResult` events, then a final text at 13:56:16. The TTS tool's pinned description and implementation auto-deliver their media, while the configured `tts.auto=inbound` voices the final response. Three HTTP TTS and three PTT media sends match this exactly. No user audio/transcript/response contents or phone identifiers were printed or retained.
- Source proposal: `tools.deny=["tts"]` in the sole OpenClaw config. Pinned 2026.9.4 tool-policy pipeline filters global deny before sender allow; native TTS auto mode is a separate media lifecycle path and remains configured. All other owner tools remain exposed under `profile=full`/owner `allow=["*"]`. Speech config test and deploy preflight assert the invariant; local pinned config validation, shell syntax and diff checks pass.
- Not applied yet. Continue with secrets scan, commit, explicit config-only candidate apply/new external rollback checkpoint, verify native final-response PTT and only one media send after a new controlled note. Subsequent A–L, latency/quality, reboot and release remain pending.
