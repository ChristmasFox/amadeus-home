# 2026-09-26：Amadeus 1.5.9 群聊日语 TTS guard 正式部署

## Release

- `VERSION=1.5.9`，release commit `3f9171f` 已 push。
- voice guard source commit `29661cd` 已包含在发布历史中。
- `./scripts/deploy-openclaw.sh --apply --build-auto` 已构建并切换受影响的 OpenClaw image。
- live OpenClaw image：`local/openclaw-amadeus:git-3f9171f47b19-20260926043743`。
- rollback checkpoint：`/DATA/AppData/openclaw/backups/amadeus-openclaw-20260926043743`。
- deploy evidence：`/Volumes/Avalon/backups/operation-skuld/deploy/amadeus-openclaw-20260926043743`。

## Live verification

- OpenClaw container：healthy，restart count 0。
- Product Radar：healthy，复用既有 image。
- 新 TTS marker 已存在于 live `/app/dist/runtime-api-zquJnB-O.mjs`。
- WhatsApp 已重新连接并监听 DM + all groups；OpenClaw health endpoint 通过。
- release owner outbox smoke 已产生 sent 文件。
- 2026-09-26 12:43 Asia/Shanghai 已观察到真实 WhatsApp direct audio 入站；同一时段也观察到真实 group audio 入站，未发送人为伪造消息。

## 已知非阻断项

- `doctor.sh` strict 检查仍报告 `media-organizer-adapter` 缺失；这是已记录的可选外部服务缺席，不影响本次 WhatsApp TTS/OpenClaw health。`DOCTOR_STRICT=0` 下其余检查全部通过。
- post-deploy log-policy/storage evidence 仍保留既有 daemon default log policy 缺失 warning；受管容器 per-container 日志限制通过，storage GC `GC=passed`。

## 验收边界

- 已完成 build、备份、切换、health、marker 和 outbox 检查。
- 仍需用户确认群聊实际收到的语音：音频应只有一段日语；可见文字应只有一组中文摘要和一组日本語行，不应出现 `日本語：中文：...`。不清空群聊记忆。
