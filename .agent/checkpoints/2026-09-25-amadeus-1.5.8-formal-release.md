# 2026-09-25 — Amadeus 1.5.8 formal release and TTS recovery

- Release commit `b54c2ed` pushed to `origin/work/amadeus-1.5.3-voice-io`; `VERSION=1.5.8`.
- Deployed with `./scripts/deploy-openclaw.sh --apply --build-auto --machine nyannyan` after verifying OrbStack only listed the `nyannyan` machine running Ubuntu Noble.
- OpenClaw image: `local/openclaw-amadeus:git-b54c2ed84ee4-20260925195233`; Product Radar reused `local/product-radar:git-d988000e1c5d-20260924130631`.
- Recovery checkpoint: `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925195233`; deploy evidence: `/Volumes/Avalon/backups/operation-skuld/deploy/amadeus-openclaw-20260925195233`.
- Build tests, typecheck, secrets scan, OpenClaw preflight, OpenClaw/Product Radar health, NAS read-only smoke, Chinese owner release notification, and owner outbox smoke passed.
- Storage identity-only check passed; bounded post-deploy maintenance passed. No generic Docker system/volume prune and no media-data change. Every managed container passed bounded Compose logging policy. `LOG_POLICY=warning` remains only because the global Docker daemon default config `/etc/docker/daemon.json` is absent; no OrbStack-wide restart was attempted. Compose config validation passed for OpenClaw, Product Radar, 9Router and Immich.
- TTS root cause: 1.5.7 voice turns caused 120s client timeouts against the self-hosted speech route; 9Router locked TTS, while the Mac Qwen engine remained occupied. Prior >320-character input took 223s; a short direct request then queued behind the stuck synthesis and timed out at 90s.
- `manage-qwen3-tts.sh --apply` updated the source/venv but its `launchctl bootstrap` returned I/O error and left the user agent stopped. Read-only checks confirmed plist validity, enabled state and no listening process. Manual `launchctl bootstrap gui/501 <plist>` recovered it. Qwen `/healthz` returned ready. Short host synthesis returned HTTP 200 `audio/mpeg` 24,812 bytes; OpenClaw container → 9Router → Qwen short synthesis returned HTTP 200 `audio/mp3` 22,796 bytes, both about 8.2 seconds.
- Actual WhatsApp inbound/outbound PTT acceptance after recovery is still pending owner test; this checkpoint does not claim that audio delivery is verified end-to-end.
