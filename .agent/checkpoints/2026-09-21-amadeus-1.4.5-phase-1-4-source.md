# Amadeus 1.4.5 Phases 1-4 source checkpoint

Date: 2026-09-21 (Asia/Shanghai)

## Implemented

- Required secret migration scripts are Git-trackable; secret inventory, export and import remain metadata-only and encrypt values outside Git.
- Scope-aware workflow emits targeted storage/Skuld workflows and affected Docker image set. `run-check.sh` bounds output, records timing and supports explicit cache reuse.
- Storage health evaluates macOS/internal, external, OrbStack guest and `/DATA` capacity with warning/critical/missing/policy states; history is retained outside Git. Scheduled maintenance is an explicit safe apply path with image/checkpoint retention and no generic prune.
- Service-aware backup registry covers OpenClaw/Product Radar/Immich/9Router/changedetection/media adapter; SQLite snapshots use the backup API; live Immich path uses custom-format `pg_dump` and `pg_restore --list`; raw emergency tar excludes live DB files.
- 9Router isolated fixture restore and scheduler fixtures pass.
- HomeLab inventory classifies active services as MIGRATE/REBUILD/EXTERNAL_DATA and records data/secret/backup/restore/verification/cutover contracts. Manifest/Runbook consistency and encrypted secret logical-id coverage are checked.
- Immich reclaim performs a fresh one-way checksum dry-run with no delete, fresh DB dump and live mount/health checks before approval; source remains pending.

## Targeted evidence

- `pnpm test:architecture` passed.
- `pnpm test:storage-runtime` passed.
- `pnpm test:migration-readiness` passed.
- `pnpm test:skuld-consistency` passed.
- service-aware backup, 9Router restore, scheduler, shell syntax, secret scan and workflow/compact-runner fixtures passed.

No Docker image build, CasaOS apply, Mac mini cutover or Immich reclaim was performed.
