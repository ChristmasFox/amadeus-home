# 2026-09-24 — 1.5.3 9Router deployment source gate

- Added `scripts/deploy-9router-speech.sh`, default dry-run, explicit `--apply` only. It reads the M204 host profile, requires a clean Git commit and protected guest DashScope/bridge key files plus regional/workspace URL before any runtime change.
- Apply order is focused tests/secrets -> exact old image export to external Avalon -> online SQLite/compose/env/secret checkpoint under protected guest `/DATA/AppData/9router/backups/` -> host ARM64 BuildKit immutable commit tag -> guest load -> canonical Compose image replacement and `up -d --no-build` -> 9Router health, bridge health and unauthenticated model 401. A failed smoke restores the previous Compose/image while retaining the checkpoint. Provider aliases and real ASR are separate post-deploy gates.
- Source lint, dry-run on actual M204 (`OLD_IMAGE=local/9router:0.5.81`), router fixture tests, workflow classification, architecture and secrets passed. No apply, image build, Compose restart, secret value exposure or WhatsApp test was performed in this phase.
- Rollback checkpoint contains an online SQLite snapshot and protected external secrets; the script's automatic failure rollback restores Compose only. Restoring the DB after provider writes requires a deliberate stop/restore/restart operation to avoid losing later data; do not claim automatic full-data rollback.
- The operator-owned `kurisu-v1` folder was created with mode 0700 but remains empty. Model and TTS token stay staged externally. DashScope key/workspace URL are still missing.
