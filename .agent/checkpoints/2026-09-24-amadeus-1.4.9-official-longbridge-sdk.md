# Amadeus 1.4.9 official Longbridge SDK checkpoint

Date: 2026-09-24 (Asia/Shanghai)
Commit: `32653e5`

## Source state

- `plugins/amadeus` uses the official `longbridge@5.1.0` Node SDK.
- The market client creates only SDK `QuoteContext` and `MarketContext`; no
  `TradeContext`, account, asset, portfolio, order, or generic market HTTP
  proxy is registered.
- SDK native optional dependencies are installed in the OpenClaw image for the
  target architecture. The Compose template persists
  `/home/node/.longbridge` at the external
  `/DATA/AppData/openclaw/data/longbridge-sdk-home/.longbridge` path.
- Operator OAuth exchange materializes the same SDK token-cache format outside
  Git. SDK refreshes are imported into canonical OAuth state without printing
  token material. The deployment checkpoint includes a protected copy of the
  SDK cache and marks its metadata sensitive.
- `openclaw_prepare.py` creates the cache boundary and restores runtime owner
  and `0600` file permissions without exposing contents.

## Sanitized verification

- `pnpm build` passed.
- `pnpm typecheck` passed.
- `pnpm test` passed: architecture, migration blockers 25/25, and all package
  suites; Amadeus suite 32/32.
- `pnpm check:architecture` passed.
- `pnpm check:secrets` passed.
- `python3 scripts/test-openclaw-workspace.py` passed.
- `node --check scripts/longbridge-oauth-authorize.mjs` passed.
- `git diff --check` passed.
- `./scripts/deploy-openclaw.sh --dry-run` passed and exited before any
  `notify-owner.sh` invocation.

## External state still pending

M204 has not yet received the protected Longbridge client id, HostAgent token,
or live OAuth state, and the user-level MacHostAgent has not yet been applied.
No release `--apply` or Docker build was performed in this checkpoint. Live
quote/session, restart persistence, host telemetry, and notification smoke
remain in `.agent/tasks/amadeus-1.4.9-live-acceptance.md`.
