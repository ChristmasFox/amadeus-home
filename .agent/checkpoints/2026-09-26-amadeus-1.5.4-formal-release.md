# 2026-09-26 — Amadeus 1.5.4 formal release

## Acceptance and release preparation

- Owner confirmed the Japanese visible-text fix is correct (“没问题了”) and explicitly requested commit, push, and formal deployment.
- Accepted behavior: WhatsApp voice replies include a Chinese summary, Japanese kanji/kana text synchronized to actual TTS speech, and Japanese PTT; typed-only replies remain Chinese-only. Previously deployed same-session voice FIFO remains part of the release.
- `./scripts/amadeus-version.sh bump patch` advanced `VERSION` from `1.5.3` to `1.5.4`; no manual version edit or unsupported bump command was used.
- `RELEASE_NOTES.md` is replaced with only this release's user-facing notes. Formal release has not yet been applied at checkpoint creation.

## Required release gates and rollout record

- Run `pnpm workflow:plan`, full `pnpm test`, `pnpm typecheck`, `pnpm check:architecture`, `pnpm check:secrets`, and `./scripts/amadeus-version.sh check`; require `git diff --check` clean.
- Commit/push release source before apply.
- Apply with `./scripts/deploy-openclaw.sh --apply --build-auto`; rebuild only images affected since live source commit and retain the external CasaOS rollback checkpoint.
- Record final immutable images, rollback checkpoint, deploy evidence, health/restart/WhatsApp state, and formal owner notification outcome below after successful apply.
- Do not claim release completion until apply and live health checks pass.
