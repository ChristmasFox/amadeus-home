# 2026-09-26 — Amadeus 1.5.4 formal release

## Acceptance and release preparation

- Owner confirmed the Japanese visible-text fix is correct (“没问题了”) and explicitly requested commit, push, and formal deployment.
- Accepted behavior: WhatsApp voice replies include a Chinese summary, Japanese kanji/kana text synchronized to actual TTS speech, and Japanese PTT; typed-only replies remain Chinese-only. Previously deployed same-session voice FIFO remains part of the release.
- `./scripts/amadeus-version.sh bump patch` advanced `VERSION` from `1.5.3` to `1.5.4`; no manual version edit or unsupported bump command was used.
- `RELEASE_NOTES.md` was replaced with only this release's user-facing notes before formal apply.

## Release gates and live result

- `pnpm test`, `pnpm typecheck`, `pnpm check:architecture`, `pnpm check:secrets`, version check, voice patch/parser tests, and `git diff --check` passed before deployment.
- Release commit `ac179bb` was pushed to `work/amadeus-1.5.3-voice-io`; formal apply used `./scripts/deploy-openclaw.sh --apply --build-auto`.
- UTC deploy stamp: `20260925161434`; in Asia/Shanghai this was September 26, 2026 00:14:34.
- OpenClaw image: `local/openclaw-amadeus:git-ac179bbd28b9-20260925161434`.
- Product Radar image reused: `local/product-radar:git-d988000e1c5d-20260924130631`.
- CasaOS rollback checkpoint: `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925161434`.
- Deploy evidence: `/Volumes/Avalon/backups/operation-skuld/deploy/amadeus-openclaw-20260925161434`.
- OpenClaw health and Product Radar health passed; NAS SSH read-only smoke passed. OpenClaw is running/healthy with restart count 0; WhatsApp is linked/connected/healthy; one OpenClaw runtime remains.
- Read-only live inspection confirms the WhatsApp voice typing lifecycle, ingress FIFO, and Japanese visible-text patch markers each occur once; pinned monitor passes `node --check`.
- Formal owner release notice was sent; `OWNER_OUTBOX_SMOKE=passed`.

## Post-deploy warnings and follow-up

- `LOG_POLICY=warning`: `log-policy.log` records `LOG_POLICY=blocked; verified external storage is unavailable`; Docker log policy was not applied.
- `POST_DEPLOY_MAINTENANCE=warning`: `storage-maintenance.log` records `REASON=verified external storage gate failed`; post-deploy storage maintenance/cleanup was not executed.
- Optional media organizer adapter was absent, so its network smoke was skipped. It does not affect the voice release; media capability acceptance remains separate.
- Track verified external storage availability/mount identity and safely re-run only the blocked log-policy/storage-maintenance gates in `.agent/tasks/2026-09-26-post-deploy-storage-gate.md`. No additional runtime change was made for these warnings.
