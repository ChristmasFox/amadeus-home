# Amadeus 1.5.0 Longbridge market calendar hotfix

Date: 2026-09-24

## State

- Live CasaOS OpenClaw/Product Radar are still the deployed 1.4.9 images from the protected
  checkpoint `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260924131115`.
- Longbridge OAuth state and the official SDK token cache are present outside Git with restrictive
  permissions; no token, client id, or private account material is recorded here.
- Direct official SDK quote and trading-session calls succeeded in the live container.
- The live `amadeus_market_session` gateway smoke returned Longbridge `301600 too many query days`.

## Change

`plugins/amadeus/src/market/client.ts` now defaults the trading-day calendar request to 180 days
before and after the current date. Explicit `start` and `end` arguments remain unchanged. `VERSION`
is `1.5.0`, and `RELEASE_NOTES.md` contains only this release.

## Verification

- `pnpm test:amadeus` passed.
- `pnpm typecheck:amadeus` passed.
- `pnpm build:amadeus` passed.
- `pnpm check:secrets` passed.
- `git diff --check` passed.

## Next action

Commit and push this hotfix, rebuild the ARM64 image, deploy with a fresh rollback checkpoint, and
repeat market session/quote, host-tool, and owner-notification smoke. The missing external
`media-organizer-adapter` and the unavailable Avalon external-storage gate remain explicitly recorded
as pending/blocked conditions; no replacement service is inferred.
