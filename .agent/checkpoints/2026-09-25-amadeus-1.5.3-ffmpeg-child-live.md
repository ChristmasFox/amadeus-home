# 2026-09-25 — Worker-safe ffmpeg child image on the sole OpenClaw runtime

- Source image built from `c64e29a`, tag `local/openclaw-amadeus:git-c64e29ad688d-20260925053214`, ARM64 guest digest `sha256:1035b74d970f9b929ee7682d78e5eeb81792a91b0ca4e4bcabe38dcc7612fb39`. The disposable network-none image fixture passed Node worker self-spawn, Longbridge native binding, actual MP3 -> 48 kHz Ogg/Opus ffmpeg child conversion and ffprobe child. The prior explicit-loader image failed SQLite worker preflight, so this corrected fixture covers both boundaries.
- After fixture source commit `2e05482`, ran explicit `deploy-openclaw.sh --apply --candidate --no-build` with the prebuilt image and live Product Radar image. The script saved protected checkpoint `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925053431`, passed the pinned OpenClaw CLI/SQLite preflight, then recreated only the single canonical OpenClaw runtime. Health, Product Radar, NAS SSH smoke and channel status passed. Candidate mode retained VERSION=1.5.2 and skipped release notification/maintenance; no 1.5.3 release was claimed.
- Live process now reports `process.execPath=/usr/local/bin/node.glibc`, still has the private library path for Longbridge, **and** a child `/usr/bin/ffmpeg -version` exits 0. WhatsApp is running/linked/connected and not busy; the native audio provider remains restricted to the internal 9Router address and `tts.auto=inbound` remains configured. 9Router's protected ASR/TTS logical routes and older checkpoint are preserved.
- **No real post-fix WhatsApp PTT receipt has been observed yet.** The owner's first voice note before this fix ended in a text-only warning. The next gate is a fresh genuine owner voice note, with inbound transcript/session, one or more TTS calls, media conversion, provider acceptance and handset receipt inspected without logging private contents. Do not mark case B or 1.5.3 complete on the basis of a child ffmpeg test.
