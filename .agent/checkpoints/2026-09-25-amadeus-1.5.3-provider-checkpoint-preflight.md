# 2026-09-25 — Provider provisioning checkpoint preflight fix

- After live 9Router image/bridge health and a real synthetic bridge -> QwenAI transcription passed, the first alias provisioning attempt authenticated through the upstream protected local CLI token but failed **before any provider/alias write**. The guest online-backup Python source embedded a single escaped newline in a host triple-quoted string, producing guest syntax failure. The attempted checkpoint directory was not created. Live 9Router/OpenClaw and the old `amadeus-asr` Chat Combo stayed unchanged.
- The embedded guest script is now a raw string; an offline test captures and compiles the exact generated script before mutation. Guest backup subprocess errors are sanitized rather than echoing command arguments. Focused tests and secrets scan passed.
- Next: commit this source fix, rerun explicit protected checkpoint and provider/alias apply, restart for STT alias cache, then direct authenticated 9Router ASR/TTS smokes. Do not claim the failed attempt provisioned models.
