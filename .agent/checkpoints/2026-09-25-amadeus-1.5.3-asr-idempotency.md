# 2026-09-25 — ASR retry coalescing and privacy-scoped observability (source phase)

- Live OpenClaw candidate and 9Router logical ASR/TTS have passed synthetic native route checks; real WhatsApp voice-note ingress still has not been observed after the operator test request. `VERSION=1.5.2`, no release claim.
- Source ASR bridge now bounds simultaneous distinct transcription work to eight, coalesces duplicate in-flight requests by SHA-256 of selected model/audio, and retains at most 128 successful transcript-only cache entries for five minutes. Failure categories remain explicit with no automatic model substitution. Cache is process-local, not a new broker or permanent audio memory.
- Structured bridge logs now include only direction, logical route, truncated 12-hex request hash, audio size/duration buckets, cache miss/hit/coalesced, outcome and latency. Neither private audio/transcript text nor credential values are logged. A mock fixture sends two concurrent identical requests behind a held upstream call, proves exactly one upstream invocation and a coalesced response, and checks log privacy. Existing success/failure/allowlist fixtures still pass.
- This source is **not yet the live 9Router image**. Rebuild/deploy under explicit checkpoint, verify live direct ASR and continued chat/API-key protection, then obtain genuine WhatsApp retry evidence for acceptance case L. Do not claim cross-restart dedup or owner delivery from this unit fixture.
