# 2026-09-25 — First real WhatsApp voice note: inbound passed, PTT failed

- The owner sent the requested real WhatsApp DM voice note. Channel status recorded a genuine inbound at 2026-09-25 13:17:50 Asia/Shanghai; the pinned WhatsApp monitor later logged a direct inbound audio event. Live 9Router ASR routed to `selfhosted-stt/qwen-audio-3.0-asr-flash` and the bridge returned `outcome=ok`. The normal Agent run proceeded; two successful TTS syntheses were observed around 13:18:27 and 13:18:53. No private voice-note bytes, transcript, sender ID or response content were copied into Git.
- **Case B outbound did not pass.** WhatsApp's media sender repeatedly failed to transcode the MP3 response using `/usr/bin/ffmpeg` (exit 127) around 13:19:27 and 13:19:49. Channel logs report a warning/text-only fallback, not an accepted PTT. Handset receipt and the reason for two TTS calls still need owner/channel evidence. Do not count this as successful voice I/O or duplicate-free delivery.
- Root cause was reproduced without user data: launching ffmpeg as a child of the current Node wrapper exits 127 with `libc.so.6: undefined symbol: __tunable_is_initialized, version GLIBC_PRIVATE`. Running ffmpeg with `LD_LIBRARY_PATH` absent exits 0. The current image's private glibc wrapper exported `LD_LIBRARY_PATH` globally for Longbridge, and the WhatsApp subprocess inherited it.
- An isolated runtime command invoked Node via `/lib/ld-amadeus.so --library-path ... /usr/local/bin/node.glibc` instead. It loaded the Longbridge SDK native binding (174 exports) while leaving child ffmpeg's environment clean and ffmpeg exited 0. Source Dockerfile now scopes the private glibc library path to Node's explicit loader. A network-none image fixture source checks both Longbridge load and MP3 -> 48 kHz Ogg/Opus child conversion; it correctly rejects the old image with `child_library_path_leaked`.
- The sole live OpenClaw remains the prior healthy candidate image `local/openclaw-amadeus:git-109d4590c6b4-20260925043803`, VERSION=1.5.2. Protected rollback checkpoint `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925043803` remains available. The fix is **not deployed yet**; build/test an immutable new image, apply with a new checkpoint, then ask owner to resend controlled voice and verify actual PTT plus typed text behavior.
