# A+MLX owner WhatsApp acceptance — 2026-09-26

- After the one-engine A+MLX/Auto candidate switch, sanitized transport logs show four real WhatsApp voice inbound→one media-send pairs at 20:47–20:50 Asia/Shanghai. Durations: 15.270s, 12.008s, 22.180s and 21.348s. Matching native TTS totals were 5.947s, 5.824s, 7.533s and 6.420s; input buckets <=80/<=80/<=160/<=80. This is a small set of actual turns, not a real p95. No actual text, transcript, voice bytes, JID or user ID were logged in Git.
- Other non-audio WhatsApp inbound events appeared between those voice events, with no extra standalone TTS synthesis; the owner explicitly replied that voice output was normal and that an ordinary typed message was sent. The owner’s response followed the explicit question about A voice likeness, one PTT, normal visible text, and typed-not-triggering-TTS, so this is the human handset-side acceptance for the candidate.
- A further voice inbound during a five-request synthetic technical benchmark was delivered as one media reply. The synthetic fifth request hit `503 tts_busy` after ~5s while the real request waited ~2.533s, then succeeded with TTS 8.968s. This was controlled-benchmark interference, not a dropped real PTT; the technical acceptance entry was changed to read-only by default with `--benchmark` separately explicit. Protected incomplete synthetic evidence remains external, not reported as a passing five-run batch.
- Same service `com.amadeus.qwen3-tts`, engine MLX, original A reference/Auto, health ready; OpenClaw candidate healthy/restart 0. MLX `vmmap` peak reached 18.4 GiB during multiple real turns, while idle footprint returned ~3.3 GiB; macOS swap used ~6.65 GiB, later ~6.08 GiB, and sampled memory pressure reported ~74% free. These samples do not prove all future memory workloads safe; continue monitoring and retain MPS rollback `/Volumes/Avalon/backups/operation-skuld/qwen3-tts/protected-performance/pre-a-mlx-candidate-20260926T123834Z`.
- Candidate voice/typed/one-PTT gate is accepted by the owner. Formal release still requires full tests, secrets, immutable image/Compose checkpoint, post-release health and real acceptance evidence; this checkpoint is not a release claim.
