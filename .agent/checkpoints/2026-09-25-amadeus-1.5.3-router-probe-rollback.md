# 2026-09-25 — 9Router speech deploy probe corrected after automatic rollback

- Operator explicitly approved the existing `qianwenaiapi.com` endpoint as the formal 1.5.3 ASR authority; the source deploy requires `--allow-qwenai-upstream` and uses the external protected key/URL. Git source `d85504c` was clean before the first apply.
- `scripts/deploy-9router-speech.sh --apply --allow-qwenai-upstream` passed protected secret preflight, exported the old image to external Avalon, wrote a guest SQLite/Compose/env/secret checkpoint `/DATA/AppData/9router/backups/voice-1.5.3-deploy-20260924T201138Z`, built/loaded immutable `local/9router:git-d85504cfeb39-20260924T201138Z`, and started the new container. Its smoke rejected the image and automatically restored the prior Compose/image. Exit was nonzero. Post-rollback production `local/9router:0.5.81` is running; host `/api/health=200`, unauthenticated `/v1/models=401`, OpenClaw remains healthy.
- Diagnosis in a separate network-none copy of the live SQLite/data with actual protected read-only secret mounts showed candidate Router health 200 and bridge health 200. The previous smoke was flawed: **inside** the 9Router container, loopback `/v1/models` returns 200 even with the preserved `requireApiKey=true` setting; from the actual **Mac published ingress**, unauthenticated `/v1/models` returns the required 401. This is a scope-of-probe error, not evidence of a broken bridge or a reason to disable API-key protection.
- The source deploy and post-alias restart probe now check both internal health endpoints but measure 401 from the Mac published ingress. Focused Python tests, shell syntax, architecture, workflow and secrets passed. No new provider/alias was written in the failed deployment.
- Next: commit/push the probe fix; re-run explicit immutable deploy and real direct 9Router ASR/TTS smokes. Do not treat rollback as a successful release or skip the full WhatsApp acceptance matrix.
