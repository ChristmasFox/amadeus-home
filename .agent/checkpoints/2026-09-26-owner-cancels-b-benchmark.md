# Owner cancels further experimental B reference testing — 2026-09-26

- Owner asked whether the live system is A+MLX and said further B testing is unnecessary. Read-only verification: live `com.amadeus.qwen3-tts` reports `ENGINE=mlx`, health 200, protected voice directory is the original A ~46s `kurisu-v1`, pinned 1.7B 8-bit MLX model selected. OpenClaw 1.6.0 image healthy/restarts 0; no B benchmark process or production B profile exists.
- B was only an experimental ~15s cropped reference for the Goal matrix. Earlier four B configuration cells timed out under the 110s watchdog; they remain incomplete in the report and numeric data, never described as successful. A separate 14.05s locally aligned private B candidate was prepared but **not benchmarked**. No TTS outage or additional user traffic was caused in response to the owner decision.
- User-authorized Goal amendment: stop further B benchmarks and do not request/perform the proposed quiet-window TTS interruption. This supersedes only Phase 4 B per-config five-run demand, not the other Goal gates. The old benchmark tasks are cancelled, not silently declared complete.
- Experimental local-only audit source/notes from unmerged `work/tts-b-matrix-audit` were preserved as Git tag `audit/tts-b-reference-20260926`; that branch was retired, leaving canonical main as production source. Protected external model/profile/metrics backups remain outside Git and are not runtime dependencies. No secrets/media were committed.
