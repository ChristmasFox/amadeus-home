# Checkpoint: HomeHub V1.1 Production Deployment

Date: 2026-09-05 (Asia/Shanghai)
Source commit: 46efb62 (`fix: make runtime release build command nounset safe`)
Pushed: `origin/main` at 46efb62
Status: DEPLOYED / HEALTHY

## Release

- Immutable image: `local/pubg-query-engine-v3:git-46efb62eba0c`
- Build digest: `sha256:874c929e20fcaa9bbf3e4f7472c21b4d60ef2f36d675a2801be420349ddd23a`
- Target: OrbStack Linux machine `ubuntu`, CasaOS
- Canonical compose: `/var/lib/casaos/apps/pubg-query-engine-v3/docker-compose.yml`
- Compose rollback backup: `/var/lib/casaos/apps/pubg-query-engine-v3/docker-compose.yml.codex-backup.20260906-012427`

## Deployment sequence

1. `git push origin main` completed successfully.
2. First `./scripts/deploy-agent-runtime.sh --apply --build` ran tests and secret scan but stopped before compose update because the build's dependency deploy attempted the unavailable host proxy `127.0.0.1:7897`.
3. Read-only verification confirmed the old production image and healthy container were unchanged after the failed attempt.
4. Fixed the release script's `set -u` empty-array expansion, committed as 46efb62, and pushed.
5. `./scripts/deploy-agent-runtime.sh --apply --build --no-proxy` completed tests, secret scan, host BuildKit build, image transfer, compose backup/update, and `docker compose up -d --no-build`.

## Verification

- `pubg-query-engine-v3` is running with the immutable image and Docker health is `healthy`.
- `/healthz` returned HTTP 200 with the expected runtime payload.
- `/homehub/health` returned HTTP 200 and `status=healthy`.
- Release test gate: 113 tests, 112 passed, 1 external-fixture skip, 0 failures.
- Secret scan passed.

## Rollback

On the Ubuntu CasaOS host, restore the image line from the compose backup (or set it back to the prior
immutable image), then run:

```sh
cd /var/lib/casaos/apps/pubg-query-engine-v3
docker compose up -d --no-build
```

After rollback, verify `/healthz`, `/homehub/health`, and the container health state.

## Safety boundary

No unrelated service was restarted. No AppData, media library, credential, or secret file was modified.
