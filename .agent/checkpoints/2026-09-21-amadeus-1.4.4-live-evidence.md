# Amadeus 1.4.4 live evidence checkpoint

- Commits `fc2047c`、`e235c54` 已 push 到 `origin/main`。
- canonical CasaOS host 为 OrbStack `ubuntu`；live 版本为 `1.4.4`。
- Immich media mount 已切到 `/Volumes/Avalon/immich/data`；source/destination 均为 77,726 files、
  130,486,455,925 bytes，cutover equivalence/checksum、fresh PostgreSQL dump、健康和样本读取均通过。
- old source `/DATA/Gallery/immich` 保留，约 130.49 GB 可回收但未回收；状态保持
  `SOURCE_RECLAIM_PENDING`。
- managed log policy、safe Docker GC、storage health/maintenance scheduler、service inventory、
  secret inventory、encrypted bundle/rehearsal 和 9Router artifact 均有 live evidence。
- `doctor.sh` 0 failure / 0 warning；`migration-readiness.sh` 输出 `OPERATION_SKULD=READY` 和
  `IMMICH_SOURCE_RECLAIM=READY_BUT_PENDING`。
- owner release/cutover `.sent.json` 已确认；Docker daemon 默认日志策略为 `local/20m/5`，原配置
  备份在 `/Volumes/Avalon/backups/operation-skuld/log-policy/daemon-20260921T110814Z`，重启后
  28 个容器恢复且 daemon/managed log audit 通过。完整无 secret 证据目录为：
  `/Volumes/Avalon/backups/operation-skuld/deploy/amadeus-1.4.4-live-20260921T111052Z`。
- scheduler fix：LaunchAgent 显式注入 OrbStack PATH，避免 launchd 的最小环境导致退出码 127；
  health 与 maintenance 最近一次实际运行均为退出码 0。
- 未执行格式化、重分区、`rsync --delete`、旧源 reclaim 或 Mac mini cutover。
