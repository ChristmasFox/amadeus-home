# 2026-09-25 — Owner ASR rejection and 30-second provider lock

- 14:34:50 Asia/Shanghai: genuine WhatsApp owner voice reached `amadeus-asr`; bridge `cache=miss`, bounded media, `outcome=transcription_failed`, ~509 ms. 9Router mapped 502 and locked the only `qwen-audio-3.0-asr-flash` account for 30 seconds. Subsequent STT requests at 14:34:51/58 were blocked by this lock. Pinned failure boundary replied with concise inability-to-recognize text; no empty-text normal Agent turn is evidenced.
- Diagnostics were ephemeral inside the private runtime only. Replaying the first ~2 s clip through the exact 16 kHz WAV protocol to the approved QwenAI upstream returned HTTP 400 with `{}` twice; an older accepted clip returned HTTP 200 with nonempty transcription. The second ~3 s clip returned upstream 200 after the lock interval. At 14:39:26, the live authenticated 9Router `amadeus-asr` route for the second clip returned HTTP 200 and nonempty transcription with a fresh bridge cache miss. No audio, transcription, credential, number or private response text was copied into Git.
- The first clip's rejection reason cannot be inferred from empty upstream error JSON. It is not a general outage or expired key. Current 9Router ASR is operational, but the bridge's upstream HTTP 400 maps to 502 and triggers the router's provider lock, multiplying one bad-input failure into later rejected attempts. Track a focused non-retryable client-error mapping/test rather than bypassing error handling or inventing a transcript.
- No runtime/config change in this checkpoint. Gateway and 9Router remain the previous candidate; SOUL/USER Japanese-default and typing config have not received a successful real retest. Owner can send a fresh short voice after cooldown, with no promise that every input will be accepted by this upstream. VERSION remains 1.5.2.
