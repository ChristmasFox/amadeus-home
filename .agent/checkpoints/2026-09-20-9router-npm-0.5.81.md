# 2026-09-20 9router npm 0.5.81 更新检查点

## Scope

- 目标运行时：OrbStack Linux machine `ubuntu` 内的 CasaOS。
- 仅更新 9router；Immich 更新证据保留在 `2026-09-20-immich-9router-update.md`。
- 未修改 Claw/OpenClaw、Caddy、frps、Cloudflare、SSH/防火墙或媒体文件。

## Live changes

- `/var/lib/casaos/apps/9router/docker-compose.yml` 当前使用 `local/9router:0.5.81`。
- 镜像由 `infra/docker/casaos/9router/Dockerfile` 构建：固定 `decolua/9router:0.5.75`
  基础镜像，安装并校验 npm `9router@0.5.81`。
- 容器使用 npm 包的 `app/custom-server.js` 直接 server 入口；端口 `20128`、
  `/DATA/AppData/9router/data`、环境变量和 secret 未改变。

## Recovery

- `/DATA/AppData/9router/backups/pre-npm-0.5.81-20260920T142239Z`
  - 包含切换前 compose、data archive 和 SHA-256 清单。
- 旧运行镜像：`local/9router:rollback-0.5.75-20260920T142239Z`。
- 原始升级恢复点仍为 `/DATA/AppData/9router/backups/pre-update-20260920T132238Z`。

## Evidence

- Dockerfile build 成功，live package version 为 `0.5.81`。
- live container：`local/9router:0.5.81`、`running`、restart count `0`。
- 本地 `dashboard`：HTTP 200；本地 `/v1/models` 无 API key：HTTP 401。
- 公网 `https://9router.nyannyan.top/dashboard`：HTTP 200；公网 `/v1/models` 无 API key：HTTP 401。
- 临时复制数据目录启动测试通过，SQLite driver 为 `better-sqlite3`；未改动生产数据副本。
