# Amadeus 1.5.1 Longbridge market calendar hotfix

Date: 2026-09-24

## State

- Live CasaOS OpenClaw/Product Radar are the deployed 1.5.0 images from the protected
  checkpoint `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260924132014`.
- Longbridge OAuth state and the official SDK token cache are present outside Git with restrictive
  permissions; no token, client id, or private account material is recorded here.
- Direct official SDK quote and trading-session calls succeeded in the live container.
- Direct SDK probing found trading-day ranges of +/-14 days succeed while +/-21 days fail with
  Longbridge `301600 too many query days`; the 1.5.0 gateway session smoke therefore remained
  unavailable.

## Change

`plugins/amadeus/src/market/client.ts` now defaults the trading-day calendar request to 14 days
before and after the current date. Explicit `start` and `end` arguments remain unchanged. `VERSION`
is `1.5.1`, and `RELEASE_NOTES.md` contains only this release.

## Verification

- `pnpm test:amadeus` passed.
- `pnpm typecheck:amadeus` passed.
- `pnpm build:amadeus` passed.
- `pnpm check:secrets` passed.
- `./scripts/amadeus-version.sh check` passed.
- `git diff --check` passed.

## Next action

Commit and push this follow-up, rebuild the ARM64 image, deploy with a fresh rollback checkpoint,
and repeat market session/quote, host-tool, and owner-notification smoke. The missing external
`media-organizer-adapter` and the unavailable Avalon external-storage gate remain explicitly recorded
as pending/blocked conditions; no replacement service is inferred.
