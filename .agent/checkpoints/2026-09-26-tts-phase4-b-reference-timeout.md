# Phase 4 B reference timeout safety gate — 2026-09-26

- The separate direct MPS A baseline matrix (A ~46s ICL × Auto/Japanese × short/normal/long, six attempts each) completed with five warmed successful samples per config; private JSONL/WAV and logs are under `/Volumes/Avalon/backups/operation-skuld/qwen3-tts/perf-matrix-20260926/results-full`.
- Initial unsupervised B ~15.4s ICL Auto short run completed 18.983s first-use and 5.946s first warm, then the third short request exceeded the unchanged 120s production TTS window while the experimental process remained live. The experimental process alone was terminated with SIGTERM (exit 143); production TTS health stayed 200. This is an actual failure sample, not a successful B latency result. No timeout was increased.
- An incomplete B listening WAV was moved to protected `prior-interrupted/`; no private file was deleted or committed. A separate supervisor now isolates each config, preserves partial JSONL/logs, and kills only its benchmark child after 110s per sample, below the production 120s window. The first supervised B Auto normal config has also timed out; the remaining matrix is still running. Do not select B or claim full matrix completion from these observations.
- All experimental files and logs are private, external to Git. The source now reports PyTorch MPS driver/current allocations for future configs; A and the earliest B samples predate that memory-field improvement and need a representative supplemental run if the final report compares unified-memory usage.
