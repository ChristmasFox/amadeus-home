# 2026-09-25 — Single OpenClaw acceptance deployment gate

- Live 9Router has the two native logical speech routes after a protected DB checkpoint and one restart: `amadeus-asr` standard transcription returned HTTP 200/nonempty text on two Mac-generated clips, and `amadeus-tts` returned HTTP 200/valid MP3 (34,316 bytes). Old Chat Combo is absent; before/after SQLite integrity is `ok`. Mac unauthenticated model listing remains 401. Evidence and rollback: `.agent/checkpoints/2026-09-25-amadeus-1.5.3-router-speech-routes.md` and protected guest `/DATA/AppData/9router/backups/voice-1.5.3-20260925T040813Z`.
- The pinned OpenClaw image is Debian 12 and lacks `ffmpeg`, while its WhatsApp plugin transcodes MP3 to native 48 kHz Ogg/Opus. The source Dockerfile now installs ffmpeg and asserts `libopus`, so an immutable OpenClaw build is required for real outbound acceptance.
- Existing `deploy-openclaw.sh --apply` correctly refuses a same-version `1.5.2` release and would emit a false 1.5.2 release notification if bypassed. The new explicit `--apply --candidate --build-auto` mode instead replaces the **single** canonical runtime, requires unchanged live VERSION, runs the normal image/secret/checkpoint/health path, and skips version announcement and release-only maintenance. It is neither a second Agent nor a 1.5.3 release. A later, separately verified patch bump/release must use the ordinary release path.
- `--dry-run --candidate --build-auto` selected OpenClaw build and Product Radar reuse. Shell syntax, pinned session-isolation fixture, candidate-mode static/dry-run fixture, architecture and secret scan passed. No OpenClaw runtime mutation happened in this phase.
- Next: commit source, build/deploy candidate with protected checkpoint, verify ffmpeg/OpenClaw config/channels/direct media behavior; then collect real WhatsApp A–L and only afterward bump VERSION, release notes and final deploy.
