# 2026-09-25 — Amadeus 1.5.5 Japanese voice-audio injection hotfix (source)

## Failure evidence and diagnosis

- Owner reported Japanese voice-audio policy still produced Chinese speech after 1.5.5 formal release.
- Read-only sanitized trace of the latest WhatsApp voice run showed assistant final output as Chinese-only: no `日本語：` line and no `[[tts:text]]`. No user or assistant text was copied to Git.
- The exact pinned OpenClaw 2026.9.4 `message_received` mapper (`toPluginMessageContext`/`deriveInboundMessageHookContext`) does not include `runId`; existing `VoiceReplyTurnTracker.record` rejected the event because it required a runId. Thus the voice Skill wasn't scoped/injected for the failed run, explaining why the model followed the Chinese language request and native inbound TTS spoke Chinese.

## Source fix

- Tracker now accepts a verified WhatsApp audio event with a `sessionKey` when the pinned message_received mapper omits runId.
- The pending marker is bound to the actual runId at `before_prompt_build`; a different run in that session will not inherit the bound marker.
- `agent_end` clears the run/session marker. Existing WhatsApp/audio-only gate, TTL (10 minutes), max entries (128) and typed-only behavior remain.
- Regression tests simulate the real pinned shape (no runId in message_received event or context), assert Japanese voice Skill injection on the subsequent run, and assert typed-only no-leak after cleanup.

## Verification

- `pnpm typecheck:amadeus` passed.
- `pnpm test:amadeus` passed (including new no-runId session fallback, binding and cleanup test).
- `pnpm build:amadeus` passed.
- `node scripts/test-openclaw-bilingual-voice.mjs`, `pnpm check:architecture`, `pnpm check:secrets`, and `git diff --check` passed.
- No new version bump or runtime mutation in source phase; live remains formal 1.5.5 until the same-version candidate apply.

## Same-version candidate deployment

- Hotfix source commit `24f9d0b` was pushed to `work/amadeus-1.5.3-voice-io`.
- Applied with `./scripts/deploy-openclaw.sh --apply --candidate --build-openclaw`; VERSION remains `1.5.5`.
- Candidate OpenClaw image: `local/openclaw-amadeus:git-24f9d0bff630-20260925175320`; Product Radar was reused.
- External rollback checkpoint: `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925175320`; its manifest includes `/DATA/AppData/openclaw/config/npm/projects` (71,206,995 bytes).
- OpenClaw is running/healthy, restart count 0; WhatsApp linked/connected/healthy; one OpenClaw runtime.
- Read-only image inspection confirms the Japanese audio Skill and session-key fallback/agent_end cleanup are bundled; WhatsApp lifecycle, FIFO and Japanese-text markers each occur once; monitor syntax check passes.

## Owner handset acceptance still pending

- No hotfix-after-deploy voice message has yet been observed.
- Send a voice note explicitly requesting Chinese speech; expected: Japanese PTT + exact Japanese visible kanji/kana line + Chinese summary. Verify typed-only language behavior remains unchanged.
- If accepted, bump to 1.5.6 through the version script and run the formal release flow.
