# 2026-09-25 — WhatsApp media agent source repair before candidate apply

- Sole live OpenClaw was the worker-safe `c64e29a` candidate; the 13:38:54 direct Opus voice ingress was processed but media upload failed repeatedly at 13:40:32–13:40:52. No PTT receipt evidence. 9Router remains the single native ASR/TTS route.
- Pinned external `socket-close-B3X9uerq.js` passes `resolveEnvFetchDispatcher` to Baileys media upload; it returns a dispatch-only Undici proxy object. The Node upload invokes `https.request({agent})` and requires `addRequest`. Reproduced the incompatible agent type and confirmed the existing `createNodeProxyAgent` supplies that method; proxy media host connectivity was separately observed with an HTTP response. No raw audio, transcript, proxy secret or key in this checkpoint.
- Added exact-anchor, idempotent `scripts/patch-openclaw-whatsapp-media-agent.mjs`, fixture and `deploy-openclaw.sh` wiring. The local pinned module copy patched twice and passed `node --check`; fixture, shell syntax, `git diff --check`, secrets scan passed. The deployment script restarts the single OpenClaw process to load the volume-installed patched module even when Compose reuses the image.
- Rollback prerequisite: deployment script's fresh external checkpoint of config/Compose/secret/data and prior image; a restore must also revert the volume-installed external WhatsApp module from protected config backup, then restart the single Gateway. This source checkpoint is not a release.
- Pending: candidate apply on the actual machine (currently OrbStack name `nyannyan`, not the documented `ubuntu`), linked/connected and API-key/health checks; new controlled voice-note admission, one PTT handset receipt and duplicate-TTS investigation; full A–L matrix, restart, quality/latency and 1.5.3 release.
