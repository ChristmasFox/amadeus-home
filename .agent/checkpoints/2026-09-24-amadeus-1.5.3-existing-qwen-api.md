# 2026-09-24 — Existing 9Router Qwen API discovery and ASR contract

- Read-only inspection of the live 9Router SQLite (no credential value printed) found one active `Qwen` custom OpenAI-compatible Responses node with a nonempty API key. Its base URL is HTTPS `*.qianwenaiapi.com/compatible-mode/v1`, **not** an Alibaba `*.aliyuncs.com` workspace. The existing `amadeus-asr` is still a Chat Combo pointing to `Qwen/qwen-audio-3.0-asr-flash`, not an STT alias.
- The exact allowlisted host was confirmed internally as `maas.qianwenaiapi.com` without printing the key. One short Mac-generated Chinese WAV (2.71 seconds, no private user audio) sent from M204 guest through the provider's multimodal-generation endpoint returned HTTP 200 and nonempty transcript; a second schema-only probe established both top-level `text` and `output.text` (12 characters). Neither response text nor key was logged or committed. This proves model/credential availability but not operator voice-note quality, 9Router STT routing or WhatsApp delivery.
- Source adapter now accepts both the Goal's official Alibaba workspace host family and this exact Qwen platform host; response normalization covers nested `output.output.text` and the observed top-level/`output.text` shapes. The model name remains a 9Router alias, not a WhatsApp special case. Offline mocked tests cover both shapes and reject an evil lookalike domain.
- Explicit `scripts/provision-9router-qwen-asr.sh --apply` copied the existing key into `/DATA/AppData/9router/secrets/asr-upstream-api-key` with uid 1000/mode 0600 and added an upstream URL to protected `9router.env`, after external-runtime checkpoint `/DATA/AppData/9router/backups/voice-qwen-source-20260924T200517Z` (consistent SQLite + env). No live container or dashboard mutation occurred; values stayed outside Git.
- **Authority difference:** the original Goal specifies Alibaba Model Studio/DashScope, whereas this pre-existing key belongs to `qianwenaiapi.com`. We do not infer that one issuer is the other. Production deploy now requires explicit `--allow-qwenai-upstream`; without a user decision, do not call the original provider-authority requirement satisfied. Existing 9Router/OpenClaw containers and `VERSION=1.5.2` remain live.
