# HomeHub V1.2 Deployment Checkpoint

- Date: 2026-09-06 (Asia/Shanghai)
- Target: OrbStack Linux machine `ubuntu`, canonical CasaOS compose.
- Source commits: `6a59a54` (V1.2 implementation), `510b6fb` (MacHostAgent compose deploy script), `99759f5` (real-host smoke acceptance), `415b6ea` (idempotent LangBot identity/callback patch).

## Runtime deployment

- Runtime image: `local/pubg-query-engine-v3:git-6a59a544bacf`.
- Runtime build/deploy compose backup: `/var/lib/casaos/apps/pubg-query-engine-v3/docker-compose.yml.codex-backup.20260906-151912`.
- HostAgent env/token mount compose backup: `/var/lib/casaos/apps/pubg-query-engine-v3/docker-compose.yml.codex-backup.20260906-152003`.
- `/var/run/docker.sock` is read-only; runtime node has socket supplementary group 104; HostAgent token is readable only by root/group 104 and is not in Git.

## LangBot deployment

- Patched image: `local/langbot-agent:415b6ea002d1-20260906-153202`.
- LangBot compose rollback: `/var/lib/casaos/apps/langbot/docker-compose.yml.codex-backup.20260906-153205`.
- Plugin API tasks: `12` `pubg-stats 3.2.4`, `13` `organize-emby 0.2.0`, `14` `macos-nas-control 0.1.4`; all reached ready state.
- The LangBot API key was read from an external ignored secret file; no value was logged or committed.

## MacHostAgent deployment

- launchd label: `com.local.homehub.mac-host-agent`.
- Listener: `0.0.0.0:49152`; endpoints remain limited to `/v1/health`, `/v1/host/status`, `/v1/cloudflared/status`.
- Live HomeHub response: hostname `Xu 的 MacBook Pro`, macOS `26.0.1 (25A362)`, model `MacBookPro18,3`, root `92.2%`, Avalon `92.4%`, 30 network entries, AC power, cloudflared `running`.
- `scripts/smoke-homehub-docker.sh` passed with 10 actual allowlisted containers and real host metrics.

## Live functional smoke

- `/v3/whoami`: Telegram private and group both resolved platform user `5501555095` as `arthur/ADMIN`; chat IDs were `5501555095` and `-5527996775`.
- HomeHub `重启 aria2` returned `hh1:confirm:<actionId>` and `hh1:cancel:<actionId>` buttons. A foreign group callback was denied, owner cancel succeeded, and PUBG routing was unaffected.
- `/v3/route` classified `确认` as HomeHub. Text fallback confirmed `重启 aria2`; action verification passed and `aria2` was `Up` after restart.
- `/status`: 14 registered services; 8 healthy, 4 degraded, 1 unhealthy (Postgres Docker healthcheck), 1 down (Glances absent), 0 unknown. LangBot/n8n/Jellyfin/media-organizer recent errors were rendered as DEGRADED diagnostics, never DOWN.

## Rollback

- Runtime: restore the recorded pubg compose backup image and run `docker compose up -d --no-build` in its CasaOS app directory.
- LangBot: restore the recorded LangBot compose backup and run `docker compose up -d --no-build`.
- HostAgent compose: restore the recorded runtime compose backup if token mount/env must be removed; keep the external token file outside Git.
