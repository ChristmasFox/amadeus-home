# 2026-09-25 — Japanese-default and no-Agent-side-send candidate live

- Source `5bc084f` committed/pushed after pinned config/policy matcher, workspace, architecture, candidate deploy fixture, shell syntax and secrets checks. Denies Agent-facing `tts` and generic `message` tools only; native final-response `tts.auto=inbound`, owner outbox and other native tools remain.
- Explicit `deploy-openclaw.sh --apply --candidate --no-build --machine nyannyan` completed. Protected external rollback `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925061743` includes pre-change config/workspace/data/secret copies; immutable image unchanged. Preflight and health passed. The previous live SOUL SHA matched previous Git seed exactly, so explicit `openclaw-workspace-sync.py --apply --approve-file SOUL.md` replaced only the persona file with the committed Japanese-default seed, then the sole OpenClaw Compose service restarted at ~14:18:21 Asia/Shanghai.
- Readback: live SOUL SHA256 `5ec929dab89f82fc3d43341b2093c10ffd5574c598be88e30ee13a793c8f40f6` equals Git seed; live `tools.deny=['tts','message']`, `tts.auto=inbound`, `tts.mode=final`. Gateway healthy, WhatsApp secondary linked/connected/healthy, 9Router unauthenticated models 401. No new voice inbound observed after restart at 14:18:41.
- Known cost: explicit Agent use of generic `message` tool is now unavailable, by design to remove unsolicited media side-sends; fixed owner notification outbox is a distinct path. Retest normal typed reply and intentional native tools under full owner profile before release.
- **Pending:** fresh Chinese-input WhatsApp voice should receive exactly one playable Japanese PTT by default, unless the request explicitly asks for Chinese. Compare sanitized Agent tool calls, ASR/TTS counts and handset report. Japanese voice quality, remaining A–L including failure/restart/reboot, latency baselines and 1.5.3 release are not complete.
