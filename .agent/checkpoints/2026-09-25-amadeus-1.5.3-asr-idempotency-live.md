# 2026-09-25 — ASR in-flight deduplication image live

- Source commit `0296df5` passed focused Node fixture, architecture, workflow and secret scan. The fixture held two identical concurrent voice requests against one mocked upstream call, proved one invocation plus a coalesced reply, and asserted that logs contain no transcript or key.
- Explicit `scripts/deploy-9router-speech.sh --apply --allow-qwenai-upstream` rebuilt only the immutable ARM64 9Router speech image, preserving the prior image and protected online SQLite/Compose/env/secret rollback checkpoint `/DATA/AppData/9router/backups/voice-1.5.3-deploy-20260925T051532Z`. Canonical 9Router is `local/9router:git-0296df534708-20260925T051532Z`; OpenClaw candidate image was not rebuilt or restarted. Published unauthenticated models still return 401.
- A fresh Mac-generated Chinese sample run through **OpenClaw's native audio capability** after the router apply returned `ok=true`, one nonempty `audio.transcription` result (13 characters) in 3.91 seconds. The live bridge produced a sanitized `direction=inbound route=amadeus-asr`, size `<=1MiB`, duration `<=15s`, `cache=miss`, `outcome=ok` record in 2.57 seconds. The request hash is deliberately not copied into Git. No user WhatsApp media or transcript contents were logged in this checkpoint.
- This proves the new image did not break the single normal ASR route and that the source privacy/log format is active. A real duplicate WhatsApp delivery, simultaneous retry and user-visible behavior remain **unverified**; process-local cache is bounded but not an across-reboot broker. TTS, owner voice quality and text fallback still require real channel acceptance. `VERSION=1.5.2`, not a release.
