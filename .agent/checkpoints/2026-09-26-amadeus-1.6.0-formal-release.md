# Amadeus 1.6.0 formal release apply — 2026-09-26

- Release source commit `c730b49` was clean/pushed. Sole `scripts/amadeus-version.sh bump patch` carried 1.5.9→1.6.0; `RELEASE_NOTES.md` is a single Chinese release. Full `pnpm build` (5s), `pnpm typecheck` (5s), and full `pnpm test` (14s under standard umask 022) passed; focused `verify:voice`, architecture, report verifier, version check, secrets and diff check passed. A first full-test attempt under a temporary umask 077 failed only because a continuity fixture expected a permission change from a standard 0755 directory; rerun with normal umask 022 passed. Protected preflight logs stay outside Git at `/Volumes/Avalon/backups/operation-skuld/release-1.6.0-preflight-20260926/`.
- Explicit `./scripts/deploy-openclaw.sh --apply --build-auto` completed: affected-only OpenClaw immutable image `local/openclaw-amadeus:git-c730b495763a-20260926130153`; Product Radar reused `local/product-radar:git-d988000e1c5d-20260924130631`. Protected CasaOS rollback `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260926130153`; deploy evidence `/Volumes/Avalon/backups/operation-skuld/deploy/amadeus-openclaw-20260926130153`. Full apply log is protected under the external preflight root.
- Post-apply OpenClaw/Product Radar healthy, restart count 0; WhatsApp linked/running/connected. Pinned TTS and WhatsApp guard markers present once each. Network-none immutable image smoke passed private Node workers, Longbridge import, MP3→Opus child and ffprobe. Owner release notification sent; owner outbox smoke passed. Native A+MLX/Auto TTS service on the same M204 LaunchAgent remains healthy, pinned assets/source verified; read-only `scripts/accept-voice.sh --apply` passed channel/engine/marker/health/memory gates without sending a message.
- Known non-Voice conditions were not hidden: optional media-organizer-adapter absent, `LOG_POLICY=warning` on daemon default, while post-deploy maintenance passed. Real post-release WhatsApp voice/typed handset acceptance is still pending; this checkpoint does not claim it. Prior exact MPS TTS rollback remains `/Volumes/Avalon/backups/operation-skuld/qwen3-tts/protected-performance/pre-a-mlx-candidate-20260926T123834Z`.
- Recovery: use the formal CasaOS checkpoint and prior immutable candidate image for Agent rollback, `docker compose up -d --no-build` followed by health/channel/real voice check. If TTS quality/memory regresses, restore the protected A/MPS LaunchAgent/service checkpoint or explicitly select `--engine mps` with the single managed service. Never enable a second Agent or parallel TTS sender.
