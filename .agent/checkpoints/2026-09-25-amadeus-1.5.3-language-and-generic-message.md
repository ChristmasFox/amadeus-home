# 2026-09-25 — Japanese default and generic-message voice duplication source

- User confirms the previous three PTTs are playable, but reports the next reply remains multiple; requests Kurisu to answer in Japanese by default, Chinese only on explicit request for Chinese output.
- On the 14:07 candidate (global `tools.deny=['tts']`), one new direct Opus owner inbound at 14:11:58 had a single successful ASR. Read-only metadata from the session's transcript shows **three** assistant `message` tool calls with `action=send` and `voiceText`, no `tts` tool call; the 9Router logs show three corresponding `amadeus-tts` requests at 14:12:05, 14:13:00, 14:13:33. Concurrent group messages were a separate session. This is an Agent-initiated generic message path, not a duplicate channel voice ingress. Content, sender IDs and voice bytes were not copied.
- Source candidate denies `tts` and `message` at the global Agent tool layer. The pinned pipeline filters deny before the owner allow layer, leaving other full-profile tools intact; native `tts.auto=inbound`, `mode=final` and the fixed owner outbox are not Agent tool invocations. Focused pinned config/policy, workspace seed, candidate-deploy and architecture checks plus secrets scan passed.
- SOUL seed alone owns the new Japanese-default persona policy; it does not include capability workflow or routing. Live SOUL was byte-identical to the old Git seed before this edit. The existing workspace sync tool intentionally preserves existing files, so candidate deployment must be followed by explicit one-file sync after protected checkpoint, then Gateway restart, without overwriting other workspace files.
- **Not yet applied** and no claim of single PTT or Japanese live answer. Next: commit/push, candidate apply with new restore point, `openclaw-workspace-sync.py` plan/approved SOUL apply, restart/health, then owner retest and phone count/language quality. Continue remaining A–L, fallback/reboot and release gates.
