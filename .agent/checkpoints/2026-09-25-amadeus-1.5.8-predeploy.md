# 2026-09-25 — Amadeus 1.5.8 audio-timeout hotfix pre-deploy

- `VERSION=1.5.8`; soft ~100-word guidance is not a target, routine Japanese speech is advised to stay below ~150 Japanese characters to fit the existing 120-second voice/TTS window. Long details belong in Chinese summary. No hard word/character cap was introduced; safety caveats remain mandatory.
- Root evidence: OpenClaw logs show `fetch timeout after 120000ms` to 9Router speech; 9Router logs show self-hosted TTS `fetch failed`/model lock. Host health remained ready, but direct short speech timed out at 90s, and prior >320-char synthesis logged 223s engine time.
- Also removes retired migration-source dependency from doctor/storage-health identity checks and validates live Immich upload mount; fixes Compose log updater detection of inline/anchor logging keys. Exact invalid post-deploy Immich file was restored from its validated pre-apply backup.
- Passed before commit: `pnpm workflow:verify` (storage/migration fixtures, Amadeus typecheck and 33 tests), version tests, voice lifecycle/bilingual directive tests, secrets scan, and `git diff --check`. Release deploy must be followed by explicit TTS LaunchAgent recovery, short-speech smoke (<120s), and WhatsApp PTT acceptance. No long timeout increase.
