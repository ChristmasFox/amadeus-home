# Amadeus 1.5.3 voice I/O remaining work

- Verify official DashScope API support for the exact `qwen-audio-3.0-asr-flash` or its current authoritative equivalent, input format/size and auth; do not confuse the 9Router Chat Combo with STT. If standard multipart is unsupported, implement/test the minimal deterministic STT protocol adapter behind 9Router's self-hosted STT alias.
- Reproducibly configure protected 9Router provider credentials and aliases (`amadeus-asr`, `amadeus-tts`), decide 0.5.81 versus 0.5.86 from direct tested evidence; preserve OpenAI proxy/LAN/key behavior.
- Obtain user-owned licensed `kurisu-v1` reference pair outside Git, generate a host token, install and real-test Qwen3-TTS MPS with Chinese/Japanese. Source code's mock unit tests do not prove actual inference.
- Add meaningful failure/idempotency integration tests for OpenClaw/WhatsApp, bounded inbound media/cleanup and ASR failure; verify pinned behavior, not merely schema.
- Make rollback checkpoint before apply; direct 9Router ASR/TTS and Docker-to-host smoke; OpenClaw deployment; run all real WhatsApp acceptance cases A–L and latency measurements, reboot recovery; only then bump patch 1.5.3, release notes, final checkpoint, commit and push.

- Runtime Base model/venv is staged and engineering MPS/HTTP smoke passed with a disposable synthesized reference. This does not satisfy the operator-owned `kurisu-v1` voice quality requirement or live launchd/reboot acceptance.

- TTS service token was generated externally (0600); no longer a missing artifact. Do not print or commit it. The production voice reference and ASR/dashboard credentials remain external operator inputs.

- Official ASR API shape is now discovered and an offline-tested 9Router-local adapter exists; still requires DashScope API key, region/workspace URL, bridge token and direct real transcript smoke. Add immutable 9Router deploy/rollback helper before any live image replacement.
- WhatsApp ASR failure boundary source patch is fixture-tested but not live; verify admitted DM failure reply, no normal Agent turn and no duplicate retry billing after deployment.

- 9Router deploy helper source is present and dry-run tested. Before actual apply, run an immutable candidate build/isolated fixture boot, provision the missing protected DashScope key/region URL and bridge key, then execute real direct ASR/health and preserve rollback evidence.

- Candidate 9Router image and network-none fixture passed; old image archived externally. Bridge token is staged (uid 1000/0600), so it is no longer an operator blocker. DashScope key/region URL, protected dashboard password and production voice reference remain missing. Do not conflate isolated fixture success with real cloud ASR or WhatsApp acceptance.

- The user-supplied private reference pair is present, protected and backed up; native TTS install/API/restart engineering smoke passed. Do not claim original voice quality, actual WhatsApp delivery, or full M204 reboot persistence. Obtain owner listening acceptance and a later original/authorized voice decision before closing the product-quality goal.

- Isolated 9Router alias fixture proves a restart is required for `amadeus-asr` on both evaluated 0.5.81/0.5.86. Provisioning source now performs it, but live backup, restart, direct real audio transcription and WhatsApp acceptance are still pending.

- Existing active Qwen API key and direct synthetic ASR are verified; upstream secret/URL are staged outside Git. This provider is `qianwenaiapi.com`, distinct from Goal-specified Alibaba Model Studio. Production use requires operator decision and `--allow-qwenai-upstream`; real 9Router and WhatsApp acceptance remain pending.

- Operator decision needed: permit `maas.qianwenaiapi.com` as the 1.5.3 formal ASR upstream instead of Goal-specified Alibaba Model Studio/DashScope? Until explicit confirmation, keep the `--allow-qwenai-upstream` cutover gate closed. Direct platform API smoke and isolated image fixture do not equal live 9Router/WhatsApp acceptance.

- Operator approved QwenAI as formal ASR authority. First 9Router image apply rolled back cleanly due an incorrect container-local 401 probe; source now probes the published Mac ingress. Commit and retry the checkpointed apply, then direct speech routes and real WhatsApp matrix.

- Live 9Router candidate and direct bridge->QwenAI synthetic ASR passed with checkpoint. Commit and run local-CLI-auth provisioning with another online DB checkpoint, restart for STT alias cache, and perform direct authenticated 9Router ASR/TTS. Then deploy OpenClaw and collect actual WhatsApp A–L evidence; do not call bridge-only smoke a full voice chain.

- Real 9Router logical ASR/TTS endpoints passed with synthetic audio; no longer a routing blocker. Build/deploy ffmpeg-enabled OpenClaw image with protected checkpoint and verify its WhatsApp voice-note conversion, then run real A–L WhatsApp matrix, latency/quality, reboot recovery, release/version and main push.

- Candidate OpenClaw single-runtime apply path is source-controlled and locally tested. Commit, deploy with `--apply --candidate --build-auto`, verify ffmpeg/libopus and live config, then obtain real WhatsApp modality/failure/reboot evidence before 1.5.3 release bump.

- Source fixes pending apply: provider-scoped OpenClaw media private-network permission and ASR-bridge-only Node env proxy. Rebuild only 9Router image; validate it with isolated child-env fixture and live QwenAI ASR, then apply OpenClaw config-only candidate and rerun native audio CLI. Do not widen browser/web SSRF or conflate the separate Codex/OpenAI proxy issue.

- Live synthetic chain now passes OpenClaw native ASR CLI and Gateway one-shot TTS via 9Router. Collect real WhatsApp owner DM matrix A–L (typed text default; Chinese/Japanese voice; tool intent; mixed follow-ups; ASR error and TTS stopped/recovered; long reply; retry idempotency) with sanitized inbound/outbound/session evidence and latency observations. Validate a real PTT and owner listening quality; no synthetic probe substitutes for this. Then reboot persistence, final 1.5.3 bump/release/notification, commit/main push.

- Long TTS request rejection and local MP3->Ogg/Opus conversion passed; still collect actual WhatsApp PTT output, visible text fallback on long/TTS-failure cases, and owner listening judgment.

- Build/deploy the bounded ASR singleflight/logging image with its rollback checkpoint and verify the live logical routes remain healthy. Fixture proves process-local concurrent dedup, not real channel retry L; collect that separately.

- New ASR singleflight/logging image is live with a protected checkpoint and post-deploy native synthetic transcript. Real WhatsApp retry/idempotency evidence L is still missing; do not substitute unit fixture or a single synthetic call.

- First real owner voice B failed at WhatsApp ffmpeg PTT send (exit 127), despite ASR/Agent/TTS success. Build/test/deploy the private-glibc-loader Dockerfile fix with a new checkpoint; obtain owner resend and handset confirmation. Investigate the two TTS calls before claiming no duplicate delivery. Preserve the earlier text-only fallback evidence without misreporting voice success.

- The explicit-loader image failed the OpenClaw SQLite worker preflight (process.execPath invalid ELF) before live Compose mutation; old candidate image remains active. Build/test/redeploy the revised Node-based ffmpeg/ffprobe clean-env wrappers while preserving Longbridge and worker self-spawn, then obtain a real WhatsApp voice resend/handset PTT confirmation.

- New ffmpeg clean-env image passed four isolated checks. Commit the corrected fixture, transfer/apply the prebuilt immutable image with a fresh checkpoint, then request a real owner WhatsApp resend and verify exactly how many PTT/text replies arrive.

- Worker-safe ffmpeg candidate is live and healthy with checkpoint. Ask owner for one fresh controlled WhatsApp voice resend and record actual PTT provider acceptance plus handset audibility; first pre-fix note remains a failed case, not acceptance. Investigate any repeated TTS/send attempts.

- 2026-09-25: Apply the pinned WhatsApp media-agent patch with a protected candidate checkpoint and single Gateway restart. Verify the live agent has `addRequest`, WhatsApp remains linked/connected, then use a **fresh** owner voice note to test actual handset PTT receipt. Diagnose the five TTS calls on the 13:38:54 attempt; do not count a text fallback or synthesis alone as success. OrbStack machine currently reports `nyannyan`, so use explicit `--machine nyannyan` after verifying target identity.

- 2026-09-25 13:56: post-patch real ingress now has three successful PTT transport-send records, but owner handset receipt/playability and quality are unverified. Reconcile expected number of spoken segments versus three media sends, then complete remaining A–L tests, restart, latency and fallback acceptance before release.

- Owner confirmed three post-patch PTTs physically received and playable. Structured session metadata attributes two to explicit Agent `tts` tool calls and one to native final auto-TTS. Candidate global `tools.deny=["tts"]` now awaits config-only apply with rollback and a new owner voice note to establish one PTT, then remaining A–L and quality/reboot gates.

- 14:07 auto-TTS-only config candidate is live and healthy with protected checkpoint. Obtain one new owner note and handset report to prove one PTT without Agent `tts` tool calls; then resume A–L, quality and reboot acceptance.

- 14:11 new owner note after denying `tts` still generated three Agent `message` tool calls with `voiceText`. Source now denies both Agent-facing paths; apply with checkpoint, explicitly synchronize SOUL for Japanese default, restart and obtain a fresh phone report proving one PTT in Japanese. Do not classify this attempt as idempotent success.

- 14:18 dual Agent-tool deny plus Japanese-default SOUL are now live with protected checkpoint and verified SHA. Obtain a fresh owner voice note and handset report: expected one PTT in Japanese, no Agent `tts`/`message` calls; test explicit Chinese reply separately, typed text, remaining A–L, quality and reboot before release.

- 14:22 trial sent one PTT, but Chinese persists due stale runtime USER preference conflicting with new SOUL. Source USER seed/guarded line migration and explicit instant/3-second typing cadence await checkpointed apply. Verify Japanese output and observed typing on fresh owner note; presence remains best-effort, then continue remaining matrix.

- 14:30 candidate has removed the conflicting live USER Chinese preference with protected backup and enabled explicit instant/3-second typing refresh. Ask owner for a fresh note; verify one Japanese PTT, playability and any three-dot observation. If composing is still absent, diagnose model-vs-TTS lifecycle and client best-effort limitations without pretending config proves visible presence.

- 14:34 first new owner clip was upstream HTTP 400 `{}`; bridge 502 triggered 9Router's 30-second single-account lock, causing later valid attempts to fail too. After cooldown, second clip passed both upstream and live logical ASR. Investigate/map upstream input rejection to a non-provider-locking contract with pinned 9Router fixture and ensure failure remains text-only; then obtain new Japanese/typing handset acceptance without assuming the original clip's content.

- 14:43 real tool-using voice still answered in Chinese despite Japanese SOUL/USER; 14:45 subsequent no-tool voice generated Japanese text after the user's complaint. Apply the stronger per-turn SOUL with backup and retest both plain and tool-using voice; do not assume prompt text alone guarantees language compliance.

- Strengthened SOUL is live after 14:48 backup/sync/restart; fresh non-delivered CLI prompt produced Japanese text. Real existing WhatsApp session with native tool use and handset speech remains to verify. Do not treat isolated CLI as user acceptance.

- Owner confirms 14:45 Japanese PTT, but input was Japanese; default-Japanese-on-Chinese-input remains unproven after 14:48 strengthened SOUL. Request a controlled Chinese voice with no language request, inspect final/one PTT and handset language; tool-using Chinese turn is a separate required retest.

- New bilingual voice source: visible one-sentence Chinese summary plus Japanese audio-only native TTS directive for non-Chinese voice replies. Build/apply plugin Skill candidate with checkpoint, then real owner test PTT+Chinese text count/order/fidelity; explicit Chinese voice should not get duplicate text, typed path stays text-only, and TTS failure falls back safely.

- Bilingual Skill candidate is live after immutable build/checkpoint; synthetic CLI produced the expected directive shape without delivery. Ask owner for one genuine voice note and verify phone receives exactly one non-Chinese PTT plus one Chinese summary text, then test explicit Chinese voice no-extra and TTS-failure fallback. Do not call CLI syntax full acceptance.

- 15:10 attempt final output contained Japanese only and no `[[tts:text]]` / Chinese prefix; one PTT, no text. SOUL had a conflicting blanket no-translation line. Source now resolves conflict and makes voice Skill mandatory in its description; candidate apply and handset retest still needed. Avoid architecture-forbidden global prompt-injection hooks.

- Automatic Skill selection failed twice; new source only injects voice-reply Skill for exact confirmed WhatsApp audio run via bounded runId tracker. Commit, build/apply with checkpoint, then real one-PTT-plus-Chinese-summary test; verify typed/Telegram unaffected, explicit Chinese case and fallback.

- Run-scoped voice Skill injection candidate is live with checkpoint `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925073459`. Request fresh controlled voice and confirm exactly one spoken PTT plus one Chinese sentence, order/playability/summary fidelity; test typed and explicit Chinese no-extra behavior and TTS/ASR failure fallback.

- Final user clarification: typed defaults to Simplified Chinese; voice always Japanese PTT plus Chinese summary. Reverted global Japanese preference; modified live USER one-line migration fixture to switch Japanese -> Chinese. Candidate deploy with new checkpoint, guarded SOUL/USER sync, then test typed Chinese-only and voice dual output/count on handset.

- 15:45 final modality candidate live with checkpoint `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925074349`; SOUL/USER now restore Chinese typed default while voice Skill requires Japanese PTT + Chinese summary. Request fresh typed and voice WhatsApp tests; confirm handset exact count/languages and one-line summary before closing acceptance.

- 15:51:48 group-voice latency: 0.405s ASR, two Agent calls 15.522s+10.685s due to `read` of already-injected Skill, M204 TTS 34.121s, media send tail 1.91s; total ~63.566s. One group media send logged, no matching separate Chinese text `Sent message`. Optimize redundant Skill read, examine session context/compaction with checkpoint, measure MPS warmup/Opus, and retest text sidecar over direct DM plus user handset confirmation.

- User confirmed Chinese summary was received on one 15:51:48 group voice; overall 63.6s. Source now tells a verified audio turn not to re-read its already-injected Skill (a redundant ~15.5s first model round). Apply with checkpoint and run comparison; then optimize Qwen3-TTS (~34.1s) separately, preserving Japanese audio quality.

- 16:15 no-read Skill optimization candidate is live with checkpoint `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925081444`. Get a fresh real voice latency sample to verify a single Agent call and whether PTT+Chinese text still works. Do not claim the estimated 15.5s saving until measured. Next separate TTS latency optimization should preserve voice quality and have a controlled warm/cold benchmark.

- No-read optimization's first direct DM benchmark: one model call 3.471s, no tool calls, TTS/MP3 35.162s, PTT at 42.462s total. Group vs DM prevents attributing all savings. Measure Qwen synth vs MP3 encode separately, examine Japanese warm/cold path and group context size, and ask whether this direct DM also delivered Chinese summary. Avoid speed tweaks that reduce Japanese quality without owner comparison.
