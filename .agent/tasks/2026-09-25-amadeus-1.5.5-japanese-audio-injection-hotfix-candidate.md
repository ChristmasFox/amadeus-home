# Apply 1.5.5 Japanese voice-audio prompt-injection hotfix candidate

- Status: source committed/pushed after gates; candidate apply and owner retest pending.
- Apply the hotfix as the one existing OpenClaw runtime, with `./scripts/deploy-openclaw.sh --apply --candidate --build-openclaw` and preserve external rollback checkpoint.
- Verify OpenClaw healthy/restarts=0, WhatsApp linked/connected, deployed Amadeus Skill includes the fixed Japanese audio rule, and no duplicate runtime.
- Owner acceptance: send a WhatsApp voice note explicitly requesting Chinese speech; expect Japanese PTT, matching visible Japanese kanji/kana line, and existing Chinese summary. Then send typed-only input and verify text behavior is unchanged with no Japanese row/TTS.
- If accepted, bump patch via `./scripts/amadeus-version.sh bump patch` to 1.5.6, replace release notes, run full release gates, commit/push, dry-run and formal `--apply --build-auto`.
