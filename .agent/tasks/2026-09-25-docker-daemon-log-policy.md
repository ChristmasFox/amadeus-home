# Complete host-wide Docker log-policy application safely

- Status: the 1.5.7 policy script first warned after an Immich duplicate-key failure; its exact pre-apply compose backup was restored and validated. 1.5.8 source now recognizes inline/anchor logging keys, and the 1.5.8 apply validated all Compose files and every managed container log policy. The only remaining warning is the absent `/etc/docker/daemon.json`, so the host-wide daemon default remains unaudited/unbounded.
- The application-specific `apply-docker-log-policy.sh --daemon-apply` restarts OrbStack. Do not run casually; keep the current SSH/container-control path and backup, then run with explicit operator apply when separately authorized. Verify all containers/health after restart. Do not mutate Immich media data or prune unknown objects.
