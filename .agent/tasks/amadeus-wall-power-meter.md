# M204 整机功率计接入（待设备信息）

用户提供带 W 读数的外部电表/智能插座型号或 Home Assistant 实体后：
确认读数范围、刷新周期、时区、离线状态与只读凭据；添加固定只读适配器，
将 `scope=wall_input` 与 SoC 估算分开；用带时间戳的读数按梯形积分计算 Wh/kWh，
采样缺口不外推。定向测试、secret scan、外部真实读数对照以及部署 checkpoint
通过后再将整机功率/累计能耗称作实测。不要用 CPU/load 或 33.3 mW 猜测整机能耗。
