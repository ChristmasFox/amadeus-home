# Apply Japanese voice lease + audio guard hotfix candidate

- Status: same-version 1.5.5 candidate deployed; owner handset acceptance pending.
- Source removes the disabled WhatsApp `message_received` dependency, uses the trusted active voice lease for Skill injection, and blocks non-kana/unknown PTT audio before delivery.
- Source `12df1ae` pushed; applied via `./scripts/deploy-openclaw.sh --apply --candidate --build-openclaw` as the one OpenClaw runtime. Image: `local/openclaw-amadeus:git-12df1ae6b2e1-20260925182336`. Rollback checkpoint: `/DATA/AppData/openclaw/backups/amadeus-openclaw-20260925182336` (WhatsApp npm projects tree present: 71,206,995 bytes).
- Live Amadeus Skill/lease helper and WhatsApp guard marker verified; monitor syntax passed. OpenClaw healthy/restarts=0, WhatsApp linked/connected, no second runtime. Exact live guard synthetic smoke passed for Chinese-audio rejection and Japanese/typed-only preservation.
- Owner acceptance: send a normal voice note with no Chinese-language request. Expected Japanese PTT + matching visible Japanese line + Chinese summary; if Japanese generation fails, expect a text-only warning and **no Chinese PTT**. Test typed-only text remains unchanged.
- After real acceptance, bump patch to 1.5.6 through the version script, replace release notes and formally deploy. Do not relabel this candidate a formal release.
