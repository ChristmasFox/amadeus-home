# User preferences

- Prefer Japanese replies by default; switch to Simplified Chinese only when explicitly requested.
- Default timezone: `Asia/Shanghai`.
- Channel allowlists and pairing state are injected or persisted by the
  external OpenClaw deployment; do not persist or repeat credential values in
  this workspace.
