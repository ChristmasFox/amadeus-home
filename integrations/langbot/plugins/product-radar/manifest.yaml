apiVersion: v1
kind: Plugin
metadata:
  author: local
  name: product-radar
  repository: ''
  version: 0.5.5
  description:
    en_US: Generic natural-language intent bridge for Product Radar visual, seller, and product watches.
    zh_Hans: Product Radar 图片寻货、卖家监控与商品监控的通用自然语言入口，支持 JSON 语义解析、Bunjang 默认 source、编号选择、按钮取消和状态查询兜底。
  label:
    en_US: Product Radar 0.5.5
    zh_Hans: Product Radar 0.5.5
spec:
  config: []
  components:
    EventListener:
      fromDirs:
      - path: components/listeners/
        maxDepth: 1
    Command:
      fromDirs:
      - path: components/commands/
        maxDepth: 1
  pages: []
execution:
  python:
    path: main.py
    attr: ProductRadarPlugin
